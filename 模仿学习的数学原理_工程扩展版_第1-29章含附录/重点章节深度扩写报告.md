# 重点章节深度扩写报告

## 1. 本轮任务范围

本轮在现有 **八篇、二十九章、九个附录** 的结构基础上进行“薄弱章节深度扩写与成书级一致性整理”，未改变篇章结构、章节顺序和附录数量。

重点扩写章节：

1. 第15章 Flow Matching：从扩散去噪到连续流场生成动作
2. 第19章 SSM 与 Mamba：长时域机器人控制的线性序列骨架
3. 第22章 World Models、World Action Models 与 JEPA：机器人要学会预测世界
4. 第24章 Preference Alignment 与 DPO：从人类接管纠偏到机器人后训练

第26–29章工程篇已做轻量检查：章节、公式编号、图片链接和工程闭环逻辑未发现需要大幅改写的问题，因此本轮保持其主体内容不变。

---

## 2. 第15章新增与增强内容

第15章由概念补充章扩写为完整教材章，重点新增：

- Flow Matching 与 Diffusion Policy 的关系；
- 从“逐步去噪”到“连续速度场”的动机；
- probability path、velocity field、ODE sampling 的数学解释；
- 条件 Flow Matching 如何生成动作块 $A_t$；
- 二维点机器人绕障碍物的 running example；
- 机械臂抓取动作块生成 running example；
- 与 π0 / flow-based action expert / VLA action head 的关系；
- 与 ACT、Diffusion Policy 的工程选型对比；
- 训练数据、动作覆盖、推理稳定性、安全约束、控制频率等局限；
- 本章公式索引与建议阅读附录。

新增/重绘图片：

- `images/图15-1_Flow_Matching与Diffusion_Policy对比.png`
- `images/图15-2_条件Flow策略生成动作.png`

---

## 3. 第19章新增与增强内容

第19章围绕“SSM/Mamba 是序列骨架，不是新的 imitation loss”进行扩写，重点新增：

- Transformer 在长时域高频控制中的复杂度瓶颈；
- 连续时间 SSM 的动力系统直觉；
- 连续 SSM 到离散 SSM 的推导；
- hidden state 作为历史摘要的工程含义；
- Mamba selective state update 的直觉；
- SSM/Mamba 与 Transformer Policy 的替代、混合和分工关系；
- SSM/Mamba 与 ACT、Diffusion、Flow 的组合方式；
- 在快慢模型中作为快模型候选的原因；
- 延迟、流式推理、历史缓存、状态重置、异常恢复等部署关注点；
- 机械臂长历史状态记忆 running example。

新增/重绘图片：

- `images/图19-1_Transformer与SSM复杂度对比.png`
- `images/图19-2_Mamba选择性状态更新控制闭环.png`

---

## 4. 第22章新增与增强内容

第22章围绕“机器人要预测动作后果”进行扩写，重点新增：

- 为什么只学 $\pi(a\mid o)$ 不够；
- 从 MDP 转移概率到 latent dynamics；
- world model / world action model 的数学表达；
- JEPA 的核心思想：预测表示而不是重建像素；
- I-JEPA / V-JEPA 思想迁移到机器人时为什么必须加入动作条件；
- WAM 如何用于候选动作块后果预测；
- 预测未来状态如何支持计划评估、安全预判、失败恢复和数据效率提升；
- 与 VLA 的分工关系；
- 与第23章快慢模型、第29章数据闭环的衔接；
- 表示可控性、预测 horizon、接触动力学、误差累积、评估困难等局限。

新增/重绘图片：

- `images/图22-1_VLA与WAM_JEPA对比.png`
- `images/图22-2_JEPA隐空间前向动力学.png`

---

## 5. 第24章新增与增强内容

第24章围绕“人类接管纠偏如何变成偏好后训练信号”进行扩写，重点新增：

- 为什么机器人后训练不能只靠 BC；
- 人类接管数据如何形成偏好对；
- 偏好对象的多粒度定义：高层计划、子目标、动作块、完整轨迹、安全投影动作；
- DPO 从 Bradley-Terry 偏好模型、KL 正则化目标到 DPO 损失的推导；
- 语言 token probability 与机器人连续动作 probability density 的区别；
- Robot-DPO / preference-based robot policy 的工程闭环；
- DPO 与 IRL、GAIL、Offline IL、第29章数据闭环的关系；
- 偏好数据质量、安全不能只靠偏好优化、连续动作建模注意事项等常见误解；
- 二维点机器人和机械臂接管纠偏 running examples。

新增/重绘图片：

- `images/图24-1_人类接管纠偏形成偏好数据.png`
- `images/图24-2_DPO用于机器人后训练流程图.png`

---

## 6. 一致性检查结果

已执行以下一致性整理：

- 全书结构保持：八篇、二十九章、九个附录；
- 未改变章节顺序；
- 已将旧的“新版七篇结构”等表述统一修正为“新版八篇结构”；
- 第15章公式编号统一为 `(15.x)`；
- 第19章公式编号统一为 `(19.x)`；
- 第22章公式编号统一为 `(22.x)`；
- 第24章公式编号统一为 `(24.x)`；
- 四个重点章节均包含“本章公式索引”，且索引编号与正文公式编号对应；
- 四个重点章节新增/重绘图片编号分别为 `图15-x`、`图19-x`、`图22-x`、`图24-x`；
- Markdown 图片链接已检查，正文引用的图片文件均存在；
- SUMMARY.md 保持可导航；
- 合并版 Markdown 已重新生成；
- zip 已测试可解压。

---

## 7. 验收项对照

| 验收项 | 结果 |
|---|---|
| 章节数：29 | 通过 |
| 附录数：9 | 通过 |
| 第15章内容明显扩充 | 通过 |
| 第19章内容明显扩充 | 通过 |
| 第22章内容明显扩充 | 通过 |
| 第24章内容明显扩充 | 通过 |
| 重点章节图片编号与章号一致 | 通过 |
| 重点章节公式索引编号与正文一致 | 通过 |
| Markdown 图片链接无断链 | 通过 |
| SUMMARY.md 可正常导航 | 通过 |
| 合并版 Markdown 可直接阅读 | 通过 |
| zip 可正常解压 | 通过 |
