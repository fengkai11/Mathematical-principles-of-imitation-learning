# 数学主线：第一篇到第六篇的成书级对齐计划

> 本文档用于承接当前讨论：检查并强化《模仿学习的数学原理》第一篇到第六篇的数学主线，使全书从“方法清单”进一步收束为“数学对象逐步升级的教材”。

---

## 1. 总判断

当前书稿已经覆盖模仿学习的主要数学内容：

```text
BC / DAgger
→ MDP / 轨迹分布 / 概率策略 / 隐变量 / CVAE
→ IRL / GAIL / Offline IL
→ ACT / Diffusion Policy / Flow Matching
→ Transformer Policy / VLA
→ World Model / World Action Model / JEPA / 世界预测进入策略系统
```

这些内容本身大体完整，但还需要一次“主线对齐式调整”。

核心问题不是缺少知识点，而是需要把每一篇之间的数学接力显性化：

```text
πθ(a|s)
→ pπ(τ)
→ ρπ(s,a)
→ pθ(A_t|o_t)
→ pθ(a_t|c_t)
→ pφ(h_{t+1:t+H}|h_t,A_t,c_t)
```

这条链要成为全书前六篇的骨架。

---

## 2. 第一篇到第六篇的统一数学主线

前六篇不是六个并列专题，而是一条逐步升级的数学问题链：

| 篇章 | 核心数学对象 | 解决的问题 | 留给下一篇的问题 |
|---|---|---|---|
| 第一篇：基本问题 | $\pi_\theta(a\mid o)$、$d^{\pi_E}$、$d^{\pi_\theta}$ | 行为克隆为什么像监督学习，又为什么闭环会失败 | 单步动作误差为什么不能解释整条轨迹失败 |
| 第二篇：序列决策与轨迹分布 | $M=(S,A,P,r,\gamma)$、$p_\pi(\tau)$、$J(\pi)$、$z$、ELBO | 策略如何通过动作改变未来，并诱导轨迹分布 | 如果单步动作拟合不够，应在什么层面匹配专家 |
| 第三篇：分布匹配与奖励视角 | $r(s,a)$、$R(\tau)$、$\rho_\pi(s,a)$、$\mathcal{C}_\epsilon(\mathcal{D})$ | 从动作拟合走向专家偏好、occupancy matching 和离线覆盖分析 | 在固定数据和复杂动作下，如何表达多峰动作分布 |
| 第四篇：现代机器人策略模型 | $A_t$、$p_\theta(A_t\mid o_t)$、去噪过程、速度场 | 从点估计升级为动作块和条件动作分布生成 | 动作生成前需要什么历史、多模态和语言上下文 |
| 第五篇：长上下文与多模态条件策略 | $h_t$、$c_t$、$c_t^{\mathrm{VLA}}$、$\pi_\theta(a_t\mid c_t)$ | 把策略条件变量从当前观测扩展为历史、视觉、语言和动作 token | 会生成动作不等于知道动作后果 |
| 第六篇：世界预测与策略验证 | $p_\phi(h_{t+1:t+H}\mid h_t,A_t,c_t)$、$F_\eta(h_t,a_t)$、风险评分 | 预测动作后果，并用于候选动作评估、短期 rollout、风险预测和安全边界 | 预测、接管、偏好和失败数据如何进入后训练与部署闭环 |

一句话总纲：

> 本书前六篇的数学主线，是从专家数据上的单步条件策略 $\pi_\theta(a\mid o)$ 出发，经过闭环轨迹分布 $p_\pi(\tau)$、occupancy measure $\rho_\pi(s,a)$、动作块条件生成分布 $p_\theta(A_t\mid o_t)$、多模态上下文条件策略 $\pi_\theta(a_t\mid c_t)$，最终进入动作条件世界预测 $p_\phi(h_{t+1:t+H}\mid h_t,A_t,c_t)$。

---

## 3. 需要调整的内容范围

本次不是推倒重写，也不是重新设计目录。调整范围是：

```text
1. 新增或更新总控文档：明确第一篇到第六篇数学对象升级链。
2. 强化全书导读：把“方法演进路线”改为“数学对象演进路线”。
3. 强化数学知识地图：删除或弱化“认知架构/快慢系统”抢主线的表达。
4. 精修第四篇导读：第四篇目前最薄，需要明确它从 occupancy / offline risk 接到动作块条件生成。
5. 精修第五篇导读：强调第五篇不是架构综述，而是条件变量 $c_t$ 的扩展。
6. 精修第六篇导读：保持“动作后果预测”主线，快慢系统只作为工程补充。
7. 更新章节目录中的第六篇描述：从“快慢系统与认知架构”改为“世界预测与策略验证”。
8. 必要时补充第一篇到第六篇主线图。
```

---

## 4. 具体实施计划

### P0：总控入口修复

- 新增本文档 `数学主线.md`。
- 更新 `全书导读_新版布局说明.md`：
  - 增加“第一篇到第六篇数学对象升级链”；
  - 把第六篇定位从“世界模型与快慢认知系统”改为“世界预测与动作后果验证”；
  - 明确快慢系统是世界预测进入实时系统后的工程补充。
- 更新 `全书数学知识地图_新版布局.md`：
  - 把第六篇节点从“世界模型与认知架构”改为“世界预测与策略验证”；
  - 增加前六篇统一公式链；
  - 修正表格中第六篇“slow/fast timescale”过强的问题。
- 更新 `全书章节目录_新版布局.md`：
  - 第六篇标题与描述对齐当前章节实际命名；
  - 删除“快慢模型是主线”的表达。

### P1：篇章导读对齐

- 第一篇、第二篇、第三篇导读已经相对完整，暂不大改，只作为上游数学链保留。
- 第四篇导读需要重点扩写：
  - 承接第三篇：offline data / occupancy / OOD 风险之后，为什么需要更强动作分布模型；
  - 明确从 $\pi_\theta(a_t\mid o_t)$ 到 $p_\theta(A_t\mid o_t)$；
  - 说明 ACT、Diffusion Policy、Flow Matching 分别在动作块、去噪生成、连续速度场上推进数学对象；
  - 引出第五篇：动作分布能生成之后，条件上下文还要扩展。
- 第五篇导读已经基本符合主线，只需保持。
- 第六篇导读已经基本符合主线，只需与目录和知识地图统一。

### P2：章节正文轻量衔接

- 第16章结尾增加向第五篇的衔接：动作生成模型解决“怎么生成动作”，但还没有解决“依据什么上下文生成动作”。
- 第18章结尾增加向第六篇的衔接：VLA 可以生成动作，但不能保证知道动作后果。
- 第21章已经基本符合当前定位，后续只检查交叉引用编号是否一致。

### P3：成书级检查

完成后检查：

```text
1. 第一篇到第六篇是否能用一条公式链讲清楚。
2. 每一篇是否说明“接住上一篇什么问题”。
3. 每一篇是否说明“留下什么问题给下一篇”。
4. 第六篇是否不再被误读为认知架构综述。
5. 快慢系统是否降级为工程补充。
6. 全书入口文档、目录、数学知识地图、篇章导读是否一致。
```

---

## 5. 本次实施优先级

本次优先完成：

```text
P0 总控入口修复
P1 第四篇导读扩写
P1 第六篇相关命名与表述统一
```

暂不进行大规模章节正文重写。正文只在必要处补充衔接，避免破坏已经审校过的章节结构。
