# 第4章 DAgger：用交互数据修正策略分布

> **本章一句话导读**：
> 第 3 章指出，Behavior Cloning 的核心问题是训练在专家分布上，执行却在学习策略自己的分布上。DAgger 的解决思路很直接：让当前策略自己跑起来，收集它实际会遇到的状态，再让专家在这些状态上补动作标签，并不断把这些数据聚合回训练集。

第 4 章的核心不是“多采一些数据”这么简单，而是回答一个数学问题：

> **既然 BC 的训练分布和执行分布不一致，我们能不能让训练数据逐渐覆盖学习策略自己会访问到的分布？**

DAgger 的答案是：可以。

---

## 4.1 回顾第 3 章的问题

第 3 章指出，BC 的训练目标主要在专家分布上：

$$
J_{BC}(\theta)
=
\mathbb{E}_{o\sim d^{\pi_E}}
\left[
\ell\left(\pi_\theta(o),a_E\right)
\right]
$$

但部署时，学习策略真正面对的是自己的访问分布：

$$
J_{exec}(\theta)
=
\mathbb{E}_{o\sim d^{\pi_\theta}}
\left[
\ell\left(\pi_\theta(o),a_E\right)
\right]
$$

问题来自：

$$
d^{\pi_E}(o)\neq d^{\pi_\theta}(o)
$$

如果只继续收集专家完美示范，训练数据可能仍然主要覆盖 $d^{\pi_E}(o)$，而不是学习策略偏离后会遇到的状态。

---

## 4.2 DAgger 基本定义

> **定义 4.1：DAgger**
>
> DAgger（Dataset Aggregation）是一种交互式模仿学习方法。它让当前学习策略在环境中执行，收集该策略实际访问到的观测，再由专家为这些观测提供动作标签，并将新数据聚合到训练集中，反复训练策略。

DAgger 的名字里有两个关键词：

```text
Dataset：数据集
Aggregation：聚合
```

它不是简单重新训练一次 BC，而是不断重复：

```text
训练策略
→ 让策略执行
→ 收集策略访问到的观测
→ 专家标注这些观测
→ 聚合数据
→ 重新训练策略
```

![图4-1 DAgger 交互式数据聚合流程](../images/ch04_dagger_loop.svg)

**图4-1 说明**：DAgger 每一轮都让当前策略执行，收集该策略实际访问到的观测，再请专家提供动作标签，并把新数据加入聚合数据集中。

---

## 4.3 DAgger 的核心数学对象

第 $i$ 轮中，当前策略记为：

> **定义 4.2：第 $i$ 轮学习策略**
>
> $\pi_i$ 表示 DAgger 第 $i$ 轮训练得到的学习策略。

当前策略执行时访问到的观测来自：

$$
o\sim d^{\pi_i}(o)
$$

专家在这些观测上给出动作标签：

$$
a_E\sim\pi_E(\cdot\mid o)
$$

聚合数据集记为：

> **定义 4.3：聚合数据集**
>
> $D_i$ 表示第 $i$ 轮结束后累计得到的训练数据集。它包含初始专家数据和后续各轮策略执行时收集的新标注数据。

初始数据集通常来自专家示范：

**公式 (4.1)：初始专家数据集**

$$
D_0=\{(o_j,a_j^E)\}_{j=1}^{N_0}
$$

其中 $a_j^E$ 表示专家动作标签。

---

## 4.4 数据集聚合公式

DAgger 的核心公式是：

**公式 (4.2)：DAgger 数据集聚合公式**

$$
D_i
=
D_{i-1}
\cup
\{(o,a_E)\mid o\sim d^{\pi_i}(o),\ a_E\sim\pi_E(\cdot\mid o)\}
$$

这个公式说明：

1. 先保留上一轮数据集 $D_{i-1}$；
2. 让当前策略 $\pi_i$ 执行；
3. 收集当前策略实际访问到的观测 $o$；
4. 请专家在这些观测上给动作标签 $a_E$；
5. 把这些新样本加入数据集。

这条公式是 DAgger 的数学核心。

> **新数据来自当前学习策略访问分布 $d^{\pi_i}$，而不是继续只来自专家访问分布 $d^{\pi_E}$。**

---

## 4.5 DAgger 的训练目标

聚合数据集得到 $D_i$ 后，训练目标仍然可以是普通监督学习形式：

**公式 (4.3)：DAgger 在聚合数据集上的训练目标**

$$
\theta_i^*
=
\arg\min_\theta
\frac{1}{|D_i|}
\sum_{(o,a_E)\in D_i}
\ell\left(\pi_\theta(o),a_E\right)
$$

这说明 DAgger 和 BC 的区别主要不在 loss：

```text
BC：在初始专家数据 D_0 上训练；
DAgger：在不断聚合的 D_i 上训练。
```

而 $D_i$ 的关键变化是：它包含学习策略自己会访问到的状态。

---

## 4.6 DAgger 算法流程

可以把 DAgger 写成如下流程：

```text
输入：初始专家数据集 D_0，专家策略或专家标注器 pi_E

1. 用 D_0 训练初始策略 pi_1
2. 对 i = 1,2,...,K：
   a. 让当前策略 pi_i 在环境中执行
   b. 收集策略访问到的观测 o
   c. 请专家为这些观测给出动作标签 a_E
   d. 将新样本加入数据集 D_i
   e. 在 D_i 上重新训练策略 pi_{i+1}

输出：最终策略 pi_K
```

![图4-2 DAgger 如何把训练分布推向学习策略访问分布](../images/ch04_distribution_correction.svg)

**图4-2 说明**：BC 主要在专家分布上训练；DAgger 通过多轮交互采样，把学习策略实际访问到的状态加入训练集，使训练数据逐步覆盖执行时可能遇到的分布。

---

## 4.7 DAgger 为什么缓解误差累积

BC 的误差累积风险可以写成：

$$
O(T^2\varepsilon)
$$

它的直觉是：早期错误会把策略带到训练数据覆盖不足的区域，后续错误继续放大。

DAgger 的修正方式是：

```text
模型会偏到哪里，就收哪里；
模型会在哪些观测下犹豫，就标哪里；
模型在哪些状态下容易继续错，就让专家在这些状态上给动作。
```

如果在线学习器在聚合数据集上持续保持较小错误，DAgger 的理论直觉是：长期错误可以从 BC 的最坏情形 $O(T^2\varepsilon)$ 改善到更接近：

**公式 (4.4)：DAgger 缓解误差累积的直觉量级**

$$
O(T\varepsilon)
$$

这里不要求初学者掌握严格证明，但要理解差别：

| 方法 | 训练数据主要来自哪里 | 误差累积风险 |
|---|---|---|
| BC | 专家分布 $d^{\pi_E}$ | 早期错误可能把策略带到训练分布外，最坏 $O(T^2\varepsilon)$ |
| DAgger | 聚合后的学习策略访问分布 | 逐步学习偏离状态下的纠偏动作，更接近 $O(T\varepsilon)$ |

一句话：

> **DAgger 让模型在自己会走到的地方继续接受专家教学。**

---

## 4.8 混合策略公式的严谨解释

DAgger 实践中，为了安全，常会使用混合策略。

**公式 (4.5)：DAgger 混合策略**

$$
\pi_i^{mix}
=
\beta_i\pi_E+(1-\beta_i)\pi_i
$$

这个公式容易被误解为“把专家动作和模型动作直接平均”。更严谨的写法是：

**公式 (4.6)：混合策略的分布解释**

$$
a_t\sim\pi_i^{mix}(\cdot\mid o_t)
=
\beta_i\pi_E(\cdot\mid o_t)
+(1-\beta_i)\pi_i(\cdot\mid o_t)
$$

它表示：

- 以概率 $\beta_i$ 使用专家策略；
- 以概率 $1-\beta_i$ 使用当前学习策略；
- 如果策略本身是概率分布，则 $\pi_i^{mix}$ 是两个动作分布的混合。

工程上通常让 $\beta_i$ 逐步减小：

```text
早期：专家比例高，保证安全；
中期：逐渐增加模型执行比例；
后期：更多暴露学习策略自己的访问分布。
```

这样既能控制风险，又能收集到模型真实会遇到的状态。

---

## 4.9 DAgger 与普通数据增强的区别

普通数据增强通常在已有样本附近做扰动：

```text
图像加噪声；
随机裁剪；
动作平滑；
轻微扰动观测。
```

这些方法可能有帮助，但它们不一定能覆盖学习策略真实会走到的分布。

DAgger 的关键是交互采样：

$$
o\sim d^{\pi_i}(o)
$$

也就是：

> **让模型自己决定它会遇到哪些观测，再请专家在这些观测上给动作。**

这就是为什么 DAgger 不是普通数据增强。

---

## 4.10 DAgger 在机械臂任务中的数据闭环

在视觉引导机械臂抓取与放置任务中，DAgger 可以这样落地：

```text
1. 先用专家示范训练一个 BC 初始策略；
2. 在低速和安全限制下让模型执行；
3. 记录模型偏离专家轨迹后的观测；
4. 专家在这些观测上给出纠偏动作；
5. 把新数据加入训练集；
6. 重新训练策略；
7. 重复执行，逐步扩大场景覆盖。
```

例如，BC 模型经常在夹爪接近轴承套时偏右。普通专家示范可能很少出现“已经偏右但还没失败”的观测。DAgger 会专门把这些观测收集下来，让专家给出“往左修正、重新对准”的动作。

---

## 4.11 DAgger 的代价与局限

DAgger 很有价值，但不免费。

### 1. 需要专家在线参与

DAgger 要让专家给学习策略访问到的状态打标签。这可能需要人工遥操作、人工接管、专家控制器或仿真 oracle。

### 2. 存在安全风险

早期策略可能很差。直接让它在真实机械臂上跑，可能造成碰撞、夹爪损坏或工件掉落。因此工程上常配合低速、限位、仿真、急停和专家接管。

### 3. 标注成本高

专家不仅要示范成功轨迹，还要给偏离状态下的纠偏动作。这类数据更有价值，但采集和标注成本也更高。

### 4. 不解决所有问题

如果策略模型容量不足、观测信息不够、动作空间设计不合理、任务本身需要长期规划，DAgger 也不能单独解决。

---

## 4.12 第一篇数学主线回顾

到第 4 章结束，第一篇的公式链可以完整串起来。

### 专家轨迹

$$
\tau=(o_1,a_1,\dots,o_T,a_T)
$$

### 专家数据集

$$
D_E=\{\tau_i\}_{i=1}^{N}
$$

### 学习策略

$$
\pi_\theta(a\mid o)
$$

### BC 的专家分布训练目标

$$
J_{BC}(\theta)
=
\mathbb{E}_{o\sim d^{\pi_E}}
\left[
\ell\left(\pi_\theta(o),a_E\right)
\right]
$$

### 部署执行目标

$$
J_{exec}(\theta)
=
\mathbb{E}_{o\sim d^{\pi_\theta}}
\left[
\ell\left(\pi_\theta(o),a_E\right)
\right]
$$

### 分布偏移

$$
d^{\pi_E}(o)\neq d^{\pi_\theta}(o)
$$

### BC 的误差累积风险

$$
O(T^2\varepsilon)
$$

### DAgger 的数据聚合

$$
D_i
=
D_{i-1}
\cup
\{(o,a_E)\mid o\sim d^{\pi_i}(o),\ a_E\sim\pi_E(\cdot\mid o)\}
$$

一句话总结：

> **第一篇的数学主线是：模仿学习从专家轨迹中学习策略；BC 在专家分布上做单步监督学习；闭环执行时策略诱导出自己的分布；分布错配导致误差累积；DAgger 通过聚合学习策略访问分布上的专家标注来缓解这个问题。**

---

## 4.13 本章总结

DAgger 的核心不是把 BC 换成一个神秘算法，而是改变数据闭环：

```text
BC：只在专家数据上训练；
DAgger：让当前策略执行，在它实际访问到的观测上请专家补标签，并把数据聚合回训练集。
```

因此，DAgger 解决的不是“损失函数不够高级”，而是“训练数据没有覆盖学习策略自己的访问分布”。

---

## 4.14 本章公式索引

### 公式 (4.1)：初始专家数据集

$$
D_0=\{(o_j,a_j^E)\}_{j=1}^{N_0}
$$

### 公式 (4.2)：DAgger 数据集聚合公式

$$
D_i
=
D_{i-1}
\cup
\{(o,a_E)\mid o\sim d^{\pi_i}(o),\ a_E\sim\pi_E(\cdot\mid o)\}
$$

### 公式 (4.3)：DAgger 在聚合数据集上的训练目标

$$
\theta_i^*
=
\arg\min_\theta
\frac{1}{|D_i|}
\sum_{(o,a_E)\in D_i}
\ell\left(\pi_\theta(o),a_E\right)
$$

### 公式 (4.4)：DAgger 缓解误差累积的直觉量级

$$
O(T\varepsilon)
$$

### 公式 (4.5)：DAgger 混合策略

$$
\pi_i^{mix}
=
\beta_i\pi_E+(1-\beta_i)\pi_i
$$

### 公式 (4.6)：混合策略的分布解释

$$
a_t\sim\pi_i^{mix}(\cdot\mid o_t)
=
\beta_i\pi_E(\cdot\mid o_t)
+(1-\beta_i)\pi_i(\cdot\mid o_t)
$$

---

## 4.15 建议阅读的附录条目

- **附录 B：概率论最小生存包**：理解 $o\sim d^{\pi_i}(o)$ 和混合分布。
- **附录 F：强化学习与序列决策基础**：理解策略执行如何诱导访问分布。
- **附录 H：实验与代码基础**：理解交互采样、闭环测试和安全评估。

---

## 4.16 思考题

1. DAgger 和普通 BC 的核心区别是什么？
2. 为什么说 DAgger 改变的是数据来源，而不一定是损失函数？
3. 请解释 $D_i=D_{i-1}\cup\{(o,a_E)\mid o\sim d^{\pi_i}(o)\}$ 的含义。
4. 为什么 DAgger 能缓解 $O(T^2\varepsilon)$ 式的误差累积？
5. 混合策略 $\pi_i^{mix}$ 为什么不是简单平均两个动作？
6. 在真实机械臂上执行 DAgger 有哪些安全风险？
7. 如果专家在线标注成本很高，你会如何近似实现 DAgger？
8. 为什么说 DAgger 不是普通数据增强？

---

## 4.17 本章配图清单

- 图4-1：DAgger 交互式数据聚合流程；
- 图4-2：DAgger 如何把训练分布推向学习策略访问分布。
