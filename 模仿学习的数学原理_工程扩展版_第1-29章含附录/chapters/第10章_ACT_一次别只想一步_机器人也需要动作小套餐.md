# 第10章：ACT：一次别只想一步，机器人也需要动作小套餐

> **本章一句话导读**：ACT 的核心不是“用了 Transformer”，而是把策略输出从单步动作推进到 action chunk，并用 CVAE 式训练让模型一次生成一段局部连贯的机器人动作。

第9章讲 Offline Imitation Learning，提醒我们：离线训练 loss 和真实部署风险不在同一个分布上。数据是否覆盖部署时会遇到的状态—动作区域，决定了策略能不能安全泛化。

从第10章开始，第四篇接着问另一个问题：即使数据覆盖还可以，策略本身是否有能力表达复杂动作？

本章只完成一个对象升级：

> 从“当前条件下预测当前动作”，升级为“当前条件下生成一段短期动作序列”。

---

## 1. 本章要解决的核心问题

单步策略可以写成：给定当前条件，预测当前动作。这个形式在第2章 Behavior Cloning 中已经出现过。它的问题不在于不能预测动作，而在于它没有显式表达局部时间结构。

以机械臂“接近—对准—轻放—松爪”为例，单步动作看起来只是每一帧的末端位移；但真正有意义的是一小段连续动作：

```text
先接近目标
→ 调整姿态
→ 减速接触
→ 轻放或插入
```

如果每一帧都独立预测，动作可能局部合理，但连起来抖动、犹豫、重复修正，甚至在接触阶段把物体硬怼进去。

ACT 要解决的问题是：

> 与其每一步都临时决定，不如让策略一次输出未来一小段动作，让动作具有短期意图和局部连续性。

注意，这并不意味着 ACT 要让机器人开环执行完整动作块。它通常仍然配合滚动执行和安全控制器使用。

---

## 2. 统一贯穿例子：精准摆入治具

本章统一使用一个机械臂例子：机器人已经通过视觉和几何方法把工件送到治具附近，接下来要完成“接近—对准—轻放—微调”。

这个任务有三个特点：

1. 单步动作不足以表达完整意图；
2. 同一条件下可能有多种合理短期动作；
3. 动作必须平滑、可执行、能在新观测下修正。

ACT 正好处在这个位置：传统几何和规划负责粗定位与安全边界，ACT 学习局部连续动作模式，底层控制器负责稳定执行。

---

## 3. 本章新数学对象

第4章已经定义动作块 $A_t$，本章不重新给动作块编号。我们直接沿用第4章的动作块写法，并把条件变量统一写成 $c_t$。这里 $c_t$ 可以包含图像、机器人状态、历史观测、任务条件、语言指令或目标位姿。

本章新增或重点实例化四个对象。

**定义 10.1：ACT 动作块生成模型**

ACT 动作块生成模型是指：在当前条件 $c_t$ 和隐变量 $z$ 给定时，生成未来动作块 $A_t$ 的条件模型。

**公式 (10.1)：ACT 条件动作块生成**

$$p_\theta(A_t\mid c_t,z)$$

其中，$A_t$ 是动作块，$c_t$ 是当前策略可见条件，$z$ 表示动作模式或短期操作风格。

**定义 10.2：ACT encoder**

ACT encoder 是训练时使用的近似后验。它可以看到当前条件和专家动作块，用来推断这段动作背后的隐变量。

**公式 (10.2)：ACT 训练时近似后验**

$$q_\phi(z\mid c_t,A_t)$$

部署时没有专家动作块，因此不能再把 $A_t$ 输入 encoder。部署阶段通常从 prior 采样或使用默认 latent，再由 decoder 生成动作块。

**定义 10.3：temporal ensemble**

temporal ensemble 是指：多个历史动作块都可能覆盖当前时刻，于是把它们对当前动作的预测加权融合，得到最终执行动作。

**定义 10.4：receding execution**

receding execution 是指：每次预测一个长度为 $H$ 的动作块，但只执行前 $H_e$ 步，然后根据新观测重新预测。

这四个对象构成本章主线：ACT 不是一个裸回归器，而是“动作块生成 + 隐变量训练 + 平滑融合 + 滚动执行”的组合。

---

## 4. 从单步动作到 action chunk：ACT 到底升级了什么

第4章已经说明，动作块不是为了把多个动作粗暴拼在一起，而是为了让策略表达一段局部动作意图。ACT 把这个对象放进现代机器人策略网络中。

在“精准摆入治具”例子里，单步预测只回答：

```text
下一帧末端往哪里动？
```

ACT 更像回答：

```text
接下来 0.5 秒如何接近、减速、对准、放入？
```

如果控制频率是 50Hz，$H=25$ 表示动作块大约覆盖 0.5 秒；$H=100$ 表示大约覆盖 2 秒。$H$ 不是越大越好。太短，动作块退化成单步动作；太长，远期动作更容易因为新观测、接触变化或目标移动而过期。

> **边界框：action chunk 不是开环一把梭**
>
> ACT 一次输出未来一段动作，不等于机器人必须把整段动作执行完。更常见的做法是执行前几步，然后重新观测、重新预测。

![图10-1 单步动作 vs action chunk 对比](../images/图13-1_单步动作_vs_action_chunk对比.png)

**图10-1 说明**：单步动作预测每一帧只输出一个动作；action chunk 一次输出未来一段动作，因此能表达局部时间结构和短期意图。当前图片文件沿用旧文件名，后续可统一重命名。

---

## 5. ACT 为什么继承 CVAE 思想

动作块不仅是“多几个时间步”。它本身也可能多模态。

同一个“放入治具”条件下，专家可能有不同动作块：

- 先靠近左侧边缘，再慢慢滑入；
- 先降低高度，再通过轻微旋转对准；
- 先试探接触，再回退一点重新进入；
- 双臂任务中，一只手稳定工件，另一只手调整位置。

如果直接对动作块做 MSE，模型可能把这些动作块逐帧平均，得到一个谁都没有真正执行过的“中间动作块”。第5章已经说明过 MSE 会倾向条件均值；到了动作块上，这个问题会沿时间维度放大。

ACT 因此继承第6章 CVAE 的思想：

```text
训练时：encoder 看专家动作块，推断 latent；
推理时：从 prior 或默认 latent 得到 z，再由 decoder 生成动作块。
```

也就是说，ACT 的隐变量不是为了增加玄学表达，而是为了让同一条件下的多种合理动作块有地方可放。

> **边界框：训练时能看答案，部署时不能**
>
> 训练时 $q_\phi(z\mid c_t,A_t)$ 可以看到专家动作块，因为它是在学习解释数据。部署时没有专家动作块，不能把未来答案塞给 encoder。否则 open-loop 结果会很好看，但那不是可部署策略。

![图10-2 ACT 结构图](../images/图13-2_ACT结构图_CVAE_Transformer_ActionChunk.png)

**图10-2 说明**：ACT 可以理解为把 CVAE 的生成对象从单步动作扩展为动作块。Transformer 负责融合视觉、机器人状态、任务条件和 latent，decoder 输出未来一段动作。当前图片文件沿用旧文件名，后续可统一重命名。

---

## 6. 从 CVAE ELBO 到 ACT loss

第6章已经给出 CVAE ELBO。本章不重新完整推导，只做一次对象替换：

```text
单步动作 a
→ 动作块 A_t

状态 s
→ 条件 c_t
```

这样，CVAE 的训练目标就变成 ACT 的动作块生成目标。

**公式 (10.3)：ACT 的 chunk loss + KL 目标**

$$\mathcal{L}_{ACT}(\theta,\phi)=\mathbb{E}_{z\sim q_\phi(z\mid c_t,A_t)}\left[\mathcal{L}_{chunk}(A_t,\hat A_t)\right]+\beta D_{KL}\left(q_\phi(z\mid c_t,A_t)\Vert p(z)\right)$$

其中，第一项要求 decoder 能重建专家动作块，第二项要求 encoder 得到的 latent 分布不要离 prior 太远。$\beta$ 控制这两者的权衡。

动作块重建项常写成逐步误差平均：

**公式 (10.4)：动作块重建损失**

$$\mathcal{L}_{chunk}(A_t,\hat A_t)=\frac{1}{H}\sum_{j=0}^{H-1}\ell(a_{t+j},\hat a_{t+j})$$

如果 $\ell$ 取平方误差，它就是动作块 MSE；如果动作维度包含夹爪、关节、末端位姿或归一化控制量，也可以对不同维度设置不同权重。

> **推导框：为什么 ACT loss 可以看成 CVAE 负 ELBO**
>
> 第6章已经说明，CVAE 训练可以理解为最大化条件对数似然的下界。ACT 只是把生成对象从 $a$ 换成 $A_t$。负的重建项变成 chunk loss，KL 项仍然约束训练时 posterior 和推理时 prior 的距离。所以 ACT loss 不是一个孤立新公式，而是 CVAE 目标在动作块上的实例化。

---

## 7. temporal ensemble：多个历史 chunk 如何融合

ACT 部署时，每个时刻都会预测一个动作块。于是，同一个当前时刻可能被多个历史动作块覆盖。temporal ensemble 的直觉是：不要只相信最近一次预测，而是把多个历史预测做加权融合。

**公式 (10.5)：temporal ensemble 加权融合**

$$\bar a_t=\frac{\sum_{i=0}^{K-1}w_i\hat a_t^{(t-i)}}{\sum_{i=0}^{K-1}w_i}$$

这里 $\hat a_t^{(t-i)}$ 表示在过去时刻 $t-i$ 预测出来、覆盖当前时刻 $t$ 的动作，$w_i$ 是权重。

> **边界框：平滑不是免费午餐**
>
> temporal ensemble 可以降低抖动，但也可能引入滞后。如果任务需要快速响应接触变化，过强的平滑会让策略慢半拍。

![图10-3 temporal ensemble 图解](../images/图13-3_temporal_ensemble图解.png)

**图10-3 说明**：多个历史动作块都对当前动作有预测，temporal ensemble 将它们加权融合，降低高频抖动。当前图片文件沿用旧文件名，后续可统一重命名。

---

## 8. receding execution：预测一段，但只执行一小段

ACT 一次预测长度为 $H$ 的动作块，但真实系统通常只执行前 $H_e$ 步，然后根据新观测重新预测。

**公式 (10.6)：滚动执行中的下一轮动作块预测**

$$\hat A_{t+H_e}=f_\theta(c_{t+H_e},z')$$

它提醒我们：动作块不是长时间开环执行，而是短期计划 + 高频反馈的折中。

![图10-4 低频决策高频执行示意图](../images/图13-4_低频决策高频执行示意图.png)

**图10-4 说明**：策略低频生成动作块，控制器高频执行前几步，随后重新观测并生成下一段。当前图片文件沿用旧文件名，后续可统一重命名。

---

## 9. 工程边界：open-loop chunk loss 不等于 closed-loop success

ACT 至少要分三层评测：

1. **open-loop chunk loss**：预测动作块和专家动作块是否接近；
2. **closed-loop success rate**：任务是否真正完成；
3. **动作平滑性和安全指标**：是否抖动、超速、超力、碰撞、触发限位。

> **边界框：离线拟合好，不代表部署成功**
>
> 第9章已经强调，离线训练损失和部署风险不在同一分布上。ACT 的 chunk loss 也是离线监督误差。它能说明模型在数据附近拟合得好，但不能保证策略在自己造成的新状态分布中仍然安全有效。

---

## 10. 读完本章，你应该能判断什么

读完本章后，你应该能判断：

1. ACT 的核心不是 Transformer，而是把动作建模对象推进到 action chunk；
2. action chunk 能表达局部时间结构，但不等于开环执行到底；
3. ACT 继承 CVAE，是为了表达多种合理动作块；
4. ACT loss 可以看成 CVAE 负 ELBO 在动作块上的实例化；
5. temporal ensemble 能减少抖动，但可能引入滞后；
6. receding execution 是让动作块保持闭环修正能力的关键；
7. open-loop chunk loss 低，不代表 closed-loop success 高。

---

## 11. 本章小结：从 ACT 到 Diffusion Policy

本章完成的对象升级是：

```text
单步动作
→ 动作块
→ 隐变量条件动作块生成
→ 滚动执行中的短期动作策略
```

ACT 把模仿学习的建模单位从“当前动作”推进到“短期动作序列”。这让策略能表达局部时间结构和短期操作意图，但它仍然受限于一次性解码、重建损失、latent 表达能力、数据质量和闭环部署风险。

下一章进入 Diffusion Policy。它和 ACT 一样关注动作块，但生成方式不同：ACT 更像一次生成动作块；Diffusion Policy 则把动作块生成改写成从噪声逐步去噪的过程。

---

## 12. 本章公式索引

### 公式 (10.1)：ACT 条件动作块生成

$$p_\theta(A_t\mid c_t,z)$$

- **作用**：把 ACT 写成给定当前条件和 latent 的动作块生成模型。
- **类型**：A 类，第四篇 ACT 的核心对象。
- **需要掌握到什么程度**：知道 ACT 生成的是动作块 $A_t$，不是单步动作 $a_t$。

### 公式 (10.2)：ACT 训练时近似后验

$$q_\phi(z\mid c_t,A_t)$$

- **作用**：训练时根据当前条件和专家动作块推断 latent。
- **类型**：B 类，承接第6章 CVAE encoder。
- **需要掌握到什么程度**：知道部署时不能把专家动作块输入 encoder。

### 公式 (10.3)：ACT 的 chunk loss + KL 目标

$$\mathcal{L}_{ACT}(\theta,\phi)=\mathbb{E}_{z\sim q_\phi(z\mid c_t,A_t)}\left[\mathcal{L}_{chunk}(A_t,\hat A_t)\right]+\beta D_{KL}\left(q_\phi(z\mid c_t,A_t)\Vert p(z)\right)$$

- **作用**：把 CVAE 训练目标实例化为 ACT 动作块训练目标。
- **类型**：A 类，ACT 训练主公式。
- **需要掌握到什么程度**：理解重建项和 KL 项分别解决什么问题。

### 公式 (10.4)：动作块重建损失

$$\mathcal{L}_{chunk}(A_t,\hat A_t)=\frac{1}{H}\sum_{j=0}^{H-1}\ell(a_{t+j},\hat a_{t+j})$$

- **作用**：衡量预测动作块和专家动作块之间的逐步误差。
- **类型**：B 类，工程训练与评测常用。
- **需要掌握到什么程度**：知道它是 open-loop imitation loss，不保证闭环成功。

### 公式 (10.5)：temporal ensemble 加权融合

$$\bar a_t=\frac{\sum_{i=0}^{K-1}w_i\hat a_t^{(t-i)}}{\sum_{i=0}^{K-1}w_i}$$

- **作用**：融合多个历史 chunk 对当前动作的预测。
- **类型**：B 类，部署平滑技巧。
- **需要掌握到什么程度**：知道它能减小抖动，也可能引入滞后。

### 公式 (10.6)：滚动执行中的下一轮动作块预测

$$\hat A_{t+H_e}=f_\theta(c_{t+H_e},z')$$

- **作用**：表示执行一小段后，根据新观测重新预测动作块。
- **类型**：B 类，连接训练动作块与闭环部署。
- **需要掌握到什么程度**：知道 ACT 通常不是一次性执行完整 chunk。

---

## 13. 本章定义索引

### 定义 10.1：ACT 动作块生成模型

给定当前条件 $c_t$ 和隐变量 $z$，生成未来动作块 $A_t$ 的条件模型。

### 定义 10.2：ACT encoder

训练时根据当前条件 $c_t$ 和专家动作块 $A_t$ 推断 latent 的近似后验模型。

### 定义 10.3：temporal ensemble

融合多个历史动作块对当前动作的预测，得到最终执行动作的方法。

### 定义 10.4：receding execution

每次预测一个动作块，但只执行前面一小段，然后根据新观测重新预测的闭环执行方式。

---

## 14. 思考题

1. ACT 的核心升级为什么不是 Transformer，而是 action chunk？
2. 在“精准摆入治具”任务中，单步动作和动作块分别能表达什么？
3. 为什么同一条件下可能存在多个合理动作块？直接对动作块做 MSE 会有什么风险？
4. 训练时 $q_\phi(z\mid c_t,A_t)$ 可以看专家动作块，为什么部署时不可以？
5. 公式 (10.3) 中 chunk loss 和 KL 项分别承担什么职责？
6. temporal ensemble 为什么可以降低抖动？它为什么也可能让机器人反应变慢？
7. receding execution 和一次性执行完整 chunk 的区别是什么？
8. 为什么 open-loop chunk loss 低，仍然不能保证 closed-loop success 高？

---

## 15. 配图清单

| 图号 | 图名 | 核心关系 | 是否已有文件 | 后续处理 |
|---|---|---|---|---|
| 图10-1 | 单步动作 vs action chunk 对比 | 说明 ACT 的对象升级：从单步动作到短期动作序列 | 沿用旧 PNG：`../images/图13-1_单步动作_vs_action_chunk对比.png` | 后续重命名为图10-1 |
| 图10-2 | ACT 结构图 | 说明 CVAE encoder、latent、decoder 与动作块输出的关系 | 沿用旧 PNG：`../images/图13-2_ACT结构图_CVAE_Transformer_ActionChunk.png` | 后续重命名为图10-2 |
| 图10-3 | temporal ensemble 图解 | 说明多个历史 chunk 如何对当前动作投票 | 沿用旧 PNG：`../images/图13-3_temporal_ensemble图解.png` | 后续重命名为图10-3 |
| 图10-4 | 低频决策高频执行示意图 | 说明预测一段、执行一小段、滚动更新的部署方式 | 沿用旧 PNG：`../images/图13-4_低频决策高频执行示意图.png` | 后续重命名为图10-4 |

---

## 16. 推荐阅读：重点看什么

- **ACT / ALOHA**：重点看 action chunk、CVAE、temporal ensemble 和实机任务设置。
- **第4章：动作块**：重点回看动作块为什么是第二篇引入的核心对象。
- **第6章：CVAE**：重点回看 encoder、decoder、prior 和 KL 项如何服务多模态动作。
- **下一章 Diffusion Policy**：重点比较“一次生成动作块”和“多步去噪生成动作块”的差异。
