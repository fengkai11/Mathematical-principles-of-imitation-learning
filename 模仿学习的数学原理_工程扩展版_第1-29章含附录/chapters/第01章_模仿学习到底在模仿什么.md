# 第1章：从专家轨迹到执行分布

> **本章一句话导读**：
> 模仿学习不是把专家轨迹背下来，也不是只在单个状态上预测专家动作，而是要从有限专家示范中学习一个能闭环执行的策略，并让这个策略诱导出的状态—动作分布接近专家分布。

---

## 1. 本章要解决的问题

模仿学习的直观说法是：专家做一遍，模型照着学。

例如，专家遥操作机械臂完成“抓取—移动—放置”，系统记录每一时刻的画面、关节状态、夹爪状态，以及专家给出的控制动作。模型训练完成后，也要在相似任务中给出合适动作。

这个说法只覆盖了表层问题。作为本书的数学起点，本章需要建立更稳定的对象链：

专家数据 $D_E$ 是有限示范；学习策略 $\pi_\theta$ 根据状态选择动作；环境转移 $P(s'\mid s,a)$ 把当前状态推进到下一状态；策略和环境共同诱导轨迹分布 $p_\pi(\tau)$，进而诱导状态访问分布 $d_\pi$ 和状态—动作分布 $\rho_\pi$。模仿学习的总目标，是让学习策略的 $\rho_\pi$ 接近专家分布 $\rho_E$。

因此，本章回答三个基础问题：

1. 专家数据在数学上是什么？
2. 学习策略在数学上是什么？
3. 为什么模仿学习最终要讨论执行分布，而不只是讨论单步动作误差？

---

## 2. 基本空间：状态与动作

轨迹、策略和执行分布都建立在两个基本空间之上：状态空间和动作空间。

### 2.1 状态空间

**定义 1.1：状态空间**

状态空间记为 $\mathcal{S}$。其中每个元素 $s\in\mathcal{S}$ 表示系统在某一时刻的状态。

在机器人任务中，状态可以包含机械臂关节角、末端位姿、夹爪开合状态、物体位姿、接触状态等信息。在自动驾驶任务中，状态可以包含车辆位置、速度、周围障碍物、车道结构等信息。

第一篇统一使用 $s$ 表示状态。真实机器人通常只能获得观测 $o$，而不是完整状态 $s$。例如，相机图像并不直接给出物体真实三维位姿，传感器也可能存在噪声和遮挡。本章暂时使用 $s$ 表示用于决策的状态信息；后续涉及视觉输入、历史信息和多模态上下文时，会再引入 $o_t$、$h_t$ 和 $c_t$。

### 2.2 动作空间

**定义 1.2：动作空间**

动作空间记为 $\mathcal{A}$。其中每个元素 $a\in\mathcal{A}$ 表示策略在某一状态下可以采取的控制动作。

在机械臂任务中，动作可以是关节角增量、末端位姿增量、夹爪开合命令，也可以是一段未来动作块。在自动驾驶任务中，动作可以是方向盘角、加速度、刹车，也可以是未来轨迹点。

动作空间可以是离散的，也可以是连续的。离散动作包括“上、下、左、右、抓取、放开”等有限选项；连续动作包括末端位姿增量、速度向量、方向盘角度等实值控制量。

后续章节主要关注机器人连续动作，但本章的主线不依赖动作空间是否连续。

### 2.3 状态—动作对

状态和动作共同组成状态—动作对 $(s,a)$。模仿学习中的训练样本、损失函数和执行分布，最终都会落在状态—动作对上。

当 $s\in\mathcal{S}$ 且 $a\in\mathcal{A}$ 时，状态—动作对属于乘积空间：

**公式 (1.1)：状态—动作空间**

$$
(s,a)\in\mathcal{S}\times\mathcal{A}
$$

这个乘积空间是后续定义专家数据、BC 数据集和占用分布的基础。

---

## 3. 专家给出的不是策略，而是有限轨迹

状态和动作确定以后，专家示范可以被写成状态—动作序列。

### 3.1 专家轨迹

**定义 1.3：专家轨迹**

一条专家轨迹是专家完成任务时产生的一段状态—动作序列，记为 $\tau$。

**公式 (1.2)：专家轨迹**

$$
\tau=(s_1,a_1,s_2,a_2,\dots,s_T,a_T,s_{T+1})
$$

其中，$s_t\in\mathcal{S}$ 是第 $t$ 个时刻的状态，$a_t\in\mathcal{A}$ 是专家在 $s_t$ 下采取的动作，$T$ 是动作步数。

轨迹末尾保留 $s_{T+1}$，表示最后一个动作 $a_T$ 执行后仍会产生下一状态。这个写法强调了模仿学习的时间结构：动作不是孤立标签，而会推进系统状态。

### 3.2 专家数据集

**定义 1.4：专家数据集**

专家数据集是多条专家轨迹组成的有限集合，记为 $D_E$。

**公式 (1.3)：专家数据集**

$$
D_E=\{\tau_i\}_{i=1}^{N}
$$

其中，$N$ 是专家轨迹条数。

在机械臂抓取与放置任务中，$D_E$ 可以包含若干条人类遥操作示范。每条示范记录了从靠近物体到完成放置的完整过程。

### 3.3 局部论断：有限轨迹不能唯一确定专家策略

专家数据集 $D_E$ 不是专家策略本身。它只是专家策略在有限任务、有限场景、有限扰动下留下的样本。因此，有限轨迹通常不能唯一确定专家在所有状态下的行为。

**论断 1.1：有限专家轨迹不能唯一确定专家策略**

给定有限专家数据集 $D_E$，通常存在多个不同策略，它们在 $D_E$ 覆盖到的状态上表现一致，但在未覆盖状态上行为不同。因此，仅凭 $D_E$ 不能唯一确定专家策略 $\pi_E$。

**直觉解释**

如果专家数据只覆盖了“机械臂已经对准物体”的状态，就无法仅凭这些样本判断专家在“夹爪碰到物体边缘”“物体被推歪”“末端姿态明显偏斜”等状态下会怎么恢复。

**证明**

设专家数据中出现过的状态集合为：

**公式 (1.4)：数据覆盖状态集合**

$$
\mathcal{S}_D=\{s_i\mid (s_i,a_i)\in D_E\}
$$

这里的 $(s_i,a_i)$ 表示从专家轨迹中拆出的状态—动作样本。

构造两个策略 $\pi_1$ 和 $\pi_2$。它们在数据覆盖到的状态上完全一致：

**公式 (1.5)：数据覆盖状态上的一致性**

$$
\pi_1(\cdot\mid s)=\pi_2(\cdot\mid s),\quad \forall s\in\mathcal{S}_D
$$

同时，取一个未覆盖状态 $s'\notin\mathcal{S}_D$，令两个策略在该状态上不同：

**公式 (1.6)：未覆盖状态上的不可识别性**

$$
\pi_1(\cdot\mid s')\neq \pi_2(\cdot\mid s')
$$

由于 $s'$ 不属于 $\mathcal{S}_D$，数据集 $D_E$ 中没有任何样本能够区分 $\pi_1$ 和 $\pi_2$ 在 $s'$ 上谁更接近专家。因此，有限数据不能唯一确定专家策略在整个状态空间上的行为。

**适用边界**

这个论断不意味着模仿学习无法泛化。它说明的是：泛化能力必须来自额外因素，例如模型结构、归纳偏置、数据覆盖、环境约束、闭环评估和后续数据收集，而不是来自有限轨迹本身的唯一确定性。

有限轨迹无法唯一确定策略，因此下一步需要明确策略本身的数学定义。

---

## 4. 策略：模型真正要学习的对象

### 4.1 策略的数学定义

**定义 1.5：策略**

策略是一个从状态到动作分布的映射。概率策略记为：

**公式 (1.7)：概率策略**

$$
\pi(a\mid s),\quad s\in\mathcal{S},\ a\in\mathcal{A}
$$

等价地，策略可以写成集合映射：

**公式 (1.8)：策略作为到动作分布的映射**

$$
\pi:\mathcal{S}\rightarrow\Delta(\mathcal{A})
$$

其中，$\Delta(\mathcal{A})$ 表示动作空间 $\mathcal{A}$ 上的概率分布集合。

这一定义允许同一个状态对应多个可能动作。对于机器人任务，这很重要：同一观测下可能存在多种合理抓取姿态、多条可行接近路径，或者不同但都能成功的放置动作。

### 4.2 专家策略与学习策略

专家背后的决策规则记为 $\pi_E$。学习模型的策略记为 $\pi_\theta$，其中 $\theta$ 是模型参数。

**公式 (1.9)：学习策略**

$$
\pi_\theta(a\mid s)
$$

如果策略是确定性的，也可以写成：

**公式 (1.10)：确定性策略**

$$
\hat a=f_\theta(s)
$$

确定性策略可以看成概率策略的特殊情况：给定状态 $s$ 时，动作分布集中在 $f_\theta(s)$ 附近。

本书后续优先使用概率策略写法 $\pi_\theta(a\mid s)$。这种写法能自然连接最大似然、负对数似然、概率策略、隐变量策略、Diffusion Policy 和 Flow Matching。

### 4.3 从轨迹到策略

专家数据 $D_E$ 是有限样本，专家策略 $\pi_E$ 是专家在状态空间上采取动作的规则。模仿学习要从 $D_E$ 中学习一个策略 $\pi_\theta$，使它在未见状态和闭环执行中也表现得像专家。

这一点把问题从“复制轨迹”推进到“学习策略”。策略一旦执行，会改变未来状态；因此还需要定义环境转移和闭环执行。

---

## 5. 闭环执行：策略和环境共同生成轨迹

### 5.1 环境转移

**定义 1.6：环境转移**

环境转移描述在当前状态 $s\in\mathcal{S}$ 下执行动作 $a\in\mathcal{A}$ 后，下一状态 $s'\in\mathcal{S}$ 如何产生。

**公式 (1.11)：环境转移分布**

$$
P(s'\mid s,a)
$$

也可以写成采样形式：

**公式 (1.12)：下一状态采样**

$$
s_{t+1}\sim P(\cdot\mid s_t,a_t)
$$

在机械臂任务中，$P(s'\mid s,a)$ 可以包含末端运动、接触、物体滑动、夹爪闭合效果、相机画面变化等复杂因素。真实系统中通常无法写出 $P$ 的解析形式，但闭环事实仍然成立：动作会改变下一状态。

### 5.2 策略动作与闭环链路

策略在状态 $s_t$ 下选择动作：

**公式 (1.13)：策略动作采样**

$$
a_t\sim\pi(a\mid s_t)
$$

动作再通过环境转移影响下一状态：

$$
s_{t+1}\sim P(\cdot\mid s_t,a_t)
$$

两者连在一起，形成最小闭环链路：

$$
s_t\rightarrow a_t\rightarrow s_{t+1}
$$

普通监督学习中的预测通常不会改变下一条输入样本，而策略的动作会改变未来状态。这是模仿学习不能直接等同于普通监督学习的根本原因。

### 5.3 策略诱导轨迹分布

**定义 1.7：初始状态分布**

初始状态分布记为 $p_0(s_1)$，表示任务开始时状态 $s_1$ 的分布。

给定 $p_0$、策略 $\pi$ 和环境转移 $P$，策略诱导出的轨迹分布为：

**公式 (1.14)：策略诱导轨迹分布**

$$
p_\pi(\tau)=p_0(s_1)\prod_{t=1}^{T}\pi(a_t\mid s_t)P(s_{t+1}\mid s_t,a_t)
$$

**推导**

轨迹 $\tau$ 由初始状态、每一步动作和每一步下一状态组成：

$$
\tau=(s_1,a_1,s_2,a_2,\dots,s_T,a_T,s_{T+1})
$$

首先，初始状态由 $p_0$ 给出：

$$
s_1\sim p_0(s_1)
$$

在第 $t$ 步，动作由策略给出：

$$
a_t\sim\pi(a_t\mid s_t)
$$

动作执行后，下一状态由环境转移给出：

$$
s_{t+1}\sim P(s_{t+1}\mid s_t,a_t)
$$

在马尔可夫假设下，下一状态只依赖当前状态和当前动作，动作只由当前状态通过策略产生。因此，整条轨迹的概率可以写成初始状态概率与每个时间步的策略项、转移项的乘积：

$$
p_\pi(\tau)=p_0(s_1)\prod_{t=1}^{T}\pi(a_t\mid s_t)P(s_{t+1}\mid s_t,a_t)
$$

这就是公式 (1.14)。

**结论**

改变策略 $\pi$ 不仅会改变每一步动作分布 $\pi(a_t\mid s_t)$，还会通过环境转移 $P(s_{t+1}\mid s_t,a_t)$ 改变后续状态分布。

---

## 6. 从轨迹分布到执行分布

策略诱导轨迹分布 $p_\pi(\tau)$ 描述整条轨迹的概率。为了比较专家和学习策略，还需要进一步关注两个对象：策略会经常到哪些状态，以及它在这些状态下会采取什么动作。

### 6.1 状态访问分布

**定义 1.8：状态访问分布**

状态访问分布 $d_\pi(s)$ 描述策略 $\pi$ 闭环执行时访问状态 $s$ 的频率或概率权重。

一种常见的折扣定义是：

**公式 (1.15)：折扣状态访问分布**

$$
d_\pi(s)=(1-\gamma)\sum_{t=1}^{\infty}\gamma^{t-1}\Pr_\pi(s_t=s)
$$

其中，$\gamma\in(0,1)$ 是折扣因子，$\Pr_\pi(s_t=s)$ 表示策略 $\pi$ 执行到第 $t$ 步时处在状态 $s$ 的概率。

也可以用递推关系理解 $d_\pi$。

**公式 (1.16)：策略诱导状态分布递推**

$$
d_\pi(s')=(1-\gamma)d_0(s')+\gamma\sum_s d_\pi(s)\sum_a\pi(a\mid s)P(s'\mid s,a)
$$

**推导**

状态 $s'$ 的访问来源可以分为两部分。

第一部分来自初始状态分布。如果系统一开始就在 $s'$，其贡献为：

$$
(1-\gamma)d_0(s')
$$

第二部分来自过去某个状态 $s$。如果策略访问了状态 $s$，在 $s$ 下选择动作 $a$，再由环境转移到 $s'$，则这一条路径的概率贡献为：

$$
d_\pi(s)\pi(a\mid s)P(s'\mid s,a)
$$

对所有可能的动作 $a$ 求和，得到从状态 $s$ 转移到 $s'$ 的总贡献：

$$
d_\pi(s)\sum_a\pi(a\mid s)P(s'\mid s,a)
$$

再对所有可能的前一状态 $s$ 求和，并乘以折扣因子 $\gamma$，得到后续访问贡献：

$$
\gamma\sum_s d_\pi(s)\sum_a\pi(a\mid s)P(s'\mid s,a)
$$

把初始贡献和后续转移贡献相加，就得到：

$$
d_\pi(s')=(1-\gamma)d_0(s')+\gamma\sum_s d_\pi(s)\sum_a\pi(a\mid s)P(s'\mid s,a)
$$

这就是公式 (1.16)。

**适用边界**

这里的递推式采用折扣访问分布写法，并假设状态和动作空间可以用求和表示。连续状态或连续动作时，求和会替换成积分；有限时域任务中，也可以使用时间平均访问分布。不同写法的细节不同，但核心结论一致：$d_\pi$ 由策略和环境转移共同决定。

### 6.2 状态—动作占用分布

**定义 1.9：状态—动作占用分布**

状态—动作占用分布，也称 occupancy measure，记为 $\rho_\pi(s,a)$，表示策略 $\pi$ 闭环执行后在状态—动作对上的联合分布。

**公式 (1.17)：状态—动作占用分布**

$$
\rho_\pi(s,a)=d_\pi(s)\pi(a\mid s)
$$

**推导**

状态—动作对 $(s,a)$ 同时发生，包含两个条件：策略执行过程中访问到状态 $s$，并且在状态 $s$ 下选择动作 $a$。

状态 $s$ 被访问的概率权重由 $d_\pi(s)$ 给出；在已访问状态 $s$ 的条件下，选择动作 $a$ 的概率由 $\pi(a\mid s)$ 给出。

根据联合概率的乘法分解：

$$
\Pr(s,a)=\Pr(s)\Pr(a\mid s)
$$

对应到策略执行分布，就是：

$$
\rho_\pi(s,a)=d_\pi(s)\pi(a\mid s)
$$

这个公式同时包含两个信息：$d_\pi(s)$ 描述策略会到哪里，$\pi(a\mid s)$ 描述策略在那里会做什么。

专家策略也诱导出专家占用分布：

**公式 (1.18)：专家状态—动作占用分布**

$$
\rho_E(s,a)=d_E(s)\pi_E(a\mid s)
$$

这里 $d_E$ 是专家执行时访问到的状态分布，$\pi_E$ 是专家策略。

至此，模仿学习的核心比较对象已经出现：学习策略的 $\rho_\pi$ 与专家的 $\rho_E$。

---

## 7. 模仿学习的总目标：匹配执行分布

模仿学习的总目标可以写成：

**公式 (1.19)：模仿学习的分布匹配目标**

$$
\min_\pi D(\rho_\pi,\rho_E)
$$

其中，$D(\cdot,\cdot)$ 表示某种分布距离。

这里暂时不规定 $D$ 的具体形式。它可以是 KL、JS、Wasserstein，也可以由判别器隐式估计。第一篇只需要建立一个母题：模仿学习的核心不是单步动作拟合，而是执行分布匹配。

**论断 1.2：单步动作拟合不是完整的模仿学习目标**

如果一个方法只约束专家访问状态上的动作预测误差，而不约束学习策略诱导出的状态访问分布 $d_\pi$，它就不能直接保证 $\rho_\pi\approx\rho_E$。

**证明**

由公式 (1.17)：

$$
\rho_\pi(s,a)=d_\pi(s)\pi(a\mid s)
$$

可以看到，$\rho_\pi$ 同时依赖 $d_\pi(s)$ 和 $\pi(a\mid s)$。

即使某个策略在专家访问状态上动作预测很准，也只是说明在 $s\sim d_E$ 的区域中，$\pi(a\mid s)$ 接近专家动作。它并不自动推出：

$$
d_\pi(s)=d_E(s)
$$

因此，也不能自动推出：

$$
\rho_\pi(s,a)=\rho_E(s,a)
$$

完整的执行分布匹配必须同时关心“会到哪里”和“会做什么”。

**适用边界**

如果任务很短、专家数据覆盖非常充分，或者系统具有强稳定性和自恢复能力，单步动作拟合可能已经足够好。但对于长时序、接触丰富、状态偏离后难以恢复的任务，仅靠专家分布上的动作拟合通常不够。

第2章会说明，Behavior Cloning 正是这种最朴素的动作拟合近似；第3章会说明，它为什么会遇到分布偏移，以及 DAgger 如何修正训练数据来源。

---

## 8. 本章收束：从对象定义到后续问题

本章建立了模仿学习的最小数学坐标系。

专家数据是有限轨迹：

$$
D_E=\{\tau_i\}_{i=1}^{N}
$$

模型要学习的是策略：

$$
\pi_\theta(a\mid s)
$$

策略闭环执行后会诱导状态—动作分布：

$$
\rho_\pi(s,a)=d_\pi(s)\pi(a\mid s)
$$

模仿学习的总目标可以理解为：

$$
\rho_\pi\approx\rho_E
$$

接下来，第2章讨论最朴素的做法：Behavior Cloning。它会把专家轨迹拆成状态—动作样本，并在专家分布上拟合动作。第3章再解释为什么这种近似会在闭环中出现问题。

---

## 9. 读完本章，你应该能判断什么

读完本章后，你应该能判断：

1. 状态空间 $\mathcal{S}$ 和动作空间 $\mathcal{A}$ 分别表示什么。
2. 专家数据 $D_E$ 为什么不等于专家策略 $\pi_E$。
3. 策略 $\pi_\theta(a\mid s)$ 和普通监督学习预测器有什么不同。
4. 环境转移 $P(s'\mid s,a)$ 为什么是闭环执行的关键对象。
5. 轨迹分布 $p_\pi(\tau)$ 如何由初始状态、策略和环境转移推导得到。
6. 状态访问分布 $d_\pi$ 的递推式来自哪些概率来源。
7. 状态—动作占用分布 $\rho_\pi$ 为什么等于 $d_\pi(s)\pi(a\mid s)$。
8. 为什么模仿学习总目标可以写成 $\min_\pi D(\rho_\pi,\rho_E)$。

---

## 10. 实践提示

> **实践提示：如果你要用代码验证本章思想，可以做什么？**

可以构造一个二维 toy environment。

例如，专家从左下角走到右上角。状态是二维位置，动作是上下左右或连续速度。收集若干条专家轨迹后，做三件事：

1. 画出专家访问过的状态点，近似得到 $d_E$。
2. 让一个随机策略或未训练策略 rollout，画出它访问到的状态点，近似得到 $d_\pi$。
3. 比较两者覆盖区域，看策略不同如何改变状态访问分布。

这个小实验能直观看到：策略不是只改变动作，它会改变自己未来进入哪些状态。

---

## 11. 本章公式索引

- **公式 (1.1)：状态—动作空间**  
  定义状态—动作对所属的乘积空间 $\mathcal{S}\times\mathcal{A}$。

- **公式 (1.2)：专家轨迹**  
  定义一条专家示范轨迹。

- **公式 (1.3)：专家数据集**  
  定义多条专家轨迹组成的有限集合 $D_E$。

- **公式 (1.4)：数据覆盖状态集合**  
  表示专家数据实际覆盖到的状态集合。

- **公式 (1.5)：数据覆盖状态上的一致性**  
  说明不同策略可以在数据覆盖区域上表现一致。

- **公式 (1.6)：未覆盖状态上的不可识别性**  
  说明有限数据无法决定未覆盖状态上的专家行为。

- **公式 (1.7)：概率策略**  
  定义状态到动作分布的基本策略写法。

- **公式 (1.8)：策略作为到动作分布的映射**  
  给出策略的集合映射形式 $\pi:\mathcal{S}\rightarrow\Delta(\mathcal{A})$。

- **公式 (1.9)：学习策略**  
  定义参数化学习策略 $\pi_\theta(a\mid s)$。

- **公式 (1.10)：确定性策略**  
  给出确定性动作回归写法 $\hat a=f_\theta(s)$。

- **公式 (1.11)：环境转移分布**  
  定义状态和动作如何决定下一状态分布。

- **公式 (1.12)：下一状态采样**  
  用采样形式表示环境转移。

- **公式 (1.13)：策略动作采样**  
  表示策略在状态下产生动作。

- **公式 (1.14)：策略诱导轨迹分布**  
  说明轨迹由初始状态、策略和环境转移共同生成。

- **公式 (1.15)：折扣状态访问分布**  
  定义策略执行时访问状态的折扣分布。

- **公式 (1.16)：策略诱导状态分布递推**  
  说明状态访问分布如何由策略和环境转移共同决定。

- **公式 (1.17)：状态—动作占用分布**  
  定义学习策略诱导出的执行分布 $\rho_\pi$。

- **公式 (1.18)：专家状态—动作占用分布**  
  定义专家执行分布 $\rho_E$。

- **公式 (1.19)：模仿学习的分布匹配目标**  
  给出第一篇和全书的核心母题。

---

## 12. 本章定义索引

- **状态空间 $\mathcal{S}$**：系统可能状态的集合。
- **动作空间 $\mathcal{A}$**：策略可选择动作的集合。
- **状态—动作对 $(s,a)$**：策略学习和执行分布分析中的基本样本单位。
- **专家轨迹 $\tau$**：专家完成任务时记录到的状态—动作序列。
- **专家数据集 $D_E$**：多条专家轨迹组成的有限样本集合。
- **策略 $\pi$**：从状态到动作分布的映射。
- **专家策略 $\pi_E$**：专家背后的决策规则。
- **学习策略 $\pi_\theta$**：模型要学习的参数化策略。
- **环境转移 $P(s'\mid s,a)$**：状态和动作如何决定下一状态的概率规律。
- **轨迹分布 $p_\pi(\tau)$**：某个策略闭环执行后产生整条轨迹的概率分布。
- **状态访问分布 $d_\pi$**：某个策略执行过程中会访问哪些状态的分布。
- **状态—动作占用分布 $\rho_\pi$**：策略在状态和动作上的联合执行分布。
- **执行分布匹配**：让学习策略诱导出的状态—动作分布接近专家分布。

---

## 13. 思考题

1. 状态空间和动作空间的定义会如何影响后续策略学习问题？
2. 为什么有限专家轨迹不能唯一确定专家策略？请用一个机器人抓取失败状态举例。
3. 在公式 (1.14) 中，如果只改变策略 $\pi$，为什么后续状态分布也会改变？
4. 公式 (1.16) 中，状态 $s'$ 的访问概率由哪两部分组成？
5. 为什么 $\rho_\pi(s,a)=d_\pi(s)\pi(a\mid s)$ 同时包含“会到哪里”和“会做什么”？
6. 如果一个模型在专家状态上动作预测很准，它一定能保证 $\rho_{\pi_\theta}\approx\rho_E$ 吗？为什么？

---

## 14. 配图清单

- **图1-1：状态空间、动作空间与轨迹元素**  
  展示 $s\in\mathcal{S}$、$a\in\mathcal{A}$ 如何组成轨迹。

- **图1-2：从专家轨迹到学习策略**  
  展示 $D_E$ 如何被用来学习 $\pi_\theta(a\mid s)$。

- **图1-3：闭环执行：策略与环境共同生成轨迹**  
  展示 $s_t\rightarrow a_t\rightarrow s_{t+1}$ 的闭环链路。

- **图1-4：状态访问分布 $d_\pi$ 与占用分布 $\rho_\pi$**  
  展示状态分布和状态—动作分布的关系。

- **图1-5：模仿学习总目标 $\rho_\pi\approx\rho_E$**  
  对比专家执行分布和学习策略执行分布。

---

## 15. 推荐阅读：重点看什么

- **DAgger 原论文**：先不看证明细节，重点看它为什么从 BC 的分布问题出发。
- **GAIL 论文**：这里只需知道 GAIL 会在后面更显式地做 occupancy matching。
- **任意一个简单 imitation learning toy project**：重点看 rollout 后状态分布如何变化，而不只是看训练误差。
