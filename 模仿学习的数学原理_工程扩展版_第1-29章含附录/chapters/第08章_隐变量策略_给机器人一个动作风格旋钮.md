# 第8章：隐变量策略：给机器人一个动作风格旋钮

> **本章一句话导读**：
> 第7章说明了同一个状态下可能存在多个合理动作，确定性策略和单峰高斯都可能不够。本章进一步引入隐变量 $z$：它像一个隐藏在动作背后的“风格旋钮”，用来表达未被直接标注的抓取方向、操作节奏、专家习惯、任务阶段或策略模式。

---

## 8.1 从第7章留下的问题开始

第7章讲到一个很容易把机器人坑惨的问题：多模态动作。

同一个杯子，可以左抓，也可以右抓；同一个障碍物，可以左绕，也可以右绕；同一个泊车状态，有人喜欢一把入库，有人喜欢多把修正。

如果仍然用一个确定性策略 $a=f_\theta(s)$ 去学这些动作，模型很可能学出平均动作。平均动作在考试分数上可能还行，在机器人里经常像一个两边都想讨好、最后谁都没讨好的中间派。

第7章给出的一个解决方向是：不要只让策略输出一个动作，而是让它输出动作分布。

但这里还有一个更深的问题：

> 多模态动作到底从哪里来？

很多时候，多模态不是动作空间里凭空长出来的蘑菇，而是因为示教数据背后有不同的风格、意图、策略模式或任务偏好。

比如：

- 抓杯子时，专家 A 喜欢从左侧抓，专家 B 喜欢从右侧抓；
- 绕障时，有的人喜欢左绕，有的人喜欢右绕；
- 双臂操作时，有的人先用左手固定再用右手操作，有的人反过来；
- 自动驾驶中，有的专家更保守，有的专家更激进；
- 泊车中，有的司机喜欢大角度一把切入，有的司机喜欢小角度多次修正。

这些差异可以看成隐藏在动作背后的变量。我们在数据中通常看不到它的名字，但它确实影响动作。

这个隐藏变量，就是本章的主角：$z$。

---

## 8.2 本章要解决的核心问题

本章围绕 8 个问题展开：

1. 什么是隐变量 $z$？为什么它被称为“隐”的？
2. 为什么多模态动作可以通过隐变量表达？
3. 什么是先验 $p(z)$？它和“随机选择一种风格”有什么关系？
4. 什么是 latent-conditioned policy $p_\theta(a\mid s,z)$？
5. 为什么最终动作分布要边缘化 $z$？
6. posterior $p(z\mid s,a)$ 在直觉上表示什么？为什么训练时它很重要？
7. 隐变量应该每一步重新采样，还是一整段轨迹保持一致？
8. 隐变量策略在工程上有什么风险？

本章的核心判断是：

> 隐变量 $z$ 不是动作本身，也不是人类强行贴的标签，而是模型用来解释动作模式差异的隐藏结构。

---

## 8.3 本章的主线定位

第7章从确定性策略推进到概率策略。本章进一步说明：当动作分布有多个模式时，可以用隐变量 $z$ 来表示隐藏模式。

```text
第7章：策略需要表达条件动作分布
        ↓
第8章：用隐变量 z 表达隐藏动作模式
        ↓
第9章：用 CVAE 学习没有标签的隐变量
        ↓
第13章：ACT 用 latent 表达动作块风格
```

本章的公式阅读抓手是：

```text
边缘化 z 不是把动作平均掉，
而是把多个潜在模式合成一个动作分布。
```

---

## 8.4 什么是隐变量？

> **定义 8.1：隐变量**
>
> 隐变量 $z$ 是指数据中没有被直接观测或标注，但会影响观测结果或动作生成过程的变量。在模仿学习中，$z$ 可以表示动作背后的隐藏风格、意图、任务阶段或策略模式。

隐变量这个名字听起来像侦探小说里的幕后黑手，其实意思很朴素：

> 数据里没直接告诉你，但它影响了你看到的动作。

在模仿学习里，我们通常看到的是状态—动作对：

**公式 (8.1)：观测到的模仿学习样本**

$$(s,a)$$

或者更一般地写成：

**公式 (8.2)：条件—动作样本**

$$(x,a)$$

其中 $x$ 可以是状态、观测、历史窗口、语言指令、目标图像或多模态上下文。

但专家为什么在这个条件下选择这个动作？数据不一定告诉你。

比如同样是看到桌上的杯子，专家选择左抓。原因可能是：

- 他习惯从左侧接近；
- 右侧有障碍物；
- 这次任务要求避开某个区域；
- 上一步动作已经让机械臂更靠左；
- 操作者只是手滑了一点，但结果仍然成功。

这些因素有些能从 $x$ 里看出来，有些看不出来，有些甚至是示教者个人习惯。对模型来说，它们都可能表现为隐藏变量 $z$。

---

## 8.5 隐变量像动作风格旋钮

如果把策略看成一个会做饭的师傅，条件 $x$ 是食材，动作 $a$ 是最后端上来的菜，那么 $z$ 像口味选择：清淡、重辣、少油、快炒、慢炖。

同样的食材，不同口味会做出不同菜。不是哪一个一定错，而是它们来自不同模式。

机器人里也一样：

- $z=\text{left-grasp}$：从左侧接近目标；
- $z=\text{right-grasp}$：从右侧接近目标；
- $z=\text{slow-safe}$：动作慢一点，离障碍物远一点；
- $z=\text{fast-direct}$：动作快一点，路径短一点；
- $z=\text{correction}$：当前阶段以修正误差为主。

当然，真实模型里的 $z$ 不一定有这么清楚的人类名字。它可能只是一个向量：

**公式 (8.3)：连续隐变量空间**

$$z\in\mathbb{R}^d$$

模型自己在这个向量空间里组织动作模式。我们希望它学出有用的结构，但不能保证每一维都能翻译成人话。

![图8-1 隐变量像动作风格旋钮](../images/图8-1_隐变量像动作风格旋钮.png)

**图8-1 说明**：

- 同一个状态 $s$ 下，不同隐变量 $z$ 对应不同动作风格；
- $z$ 不是动作本身，而是影响动作生成的隐藏模式；
- 隐变量策略的目标不是把多种动作平均掉，而是保留多种可执行模式。

---

## 8.6 先验：推理时从哪里拿到 z？

> **定义 8.2：隐变量先验**
>
> 先验 $p(z)$ 描述在还没有看到具体动作之前，隐变量 $z$ 可能取哪些值以及各自的概率。

在推理时，机器人没有专家动作可以参考。它只能根据某种规则选择或采样 $z$。

最常见的写法是：

**公式 (8.4)：从先验采样隐变量**

$$z\sim p(z)$$

如果 $z$ 是连续向量，常用标准高斯作为先验：

**公式 (8.5)：标准高斯先验**

$$p(z)=\mathcal{N}(0,I)$$

这个公式读作：隐变量 $z$ 来自均值为 0、协方差为单位矩阵的高斯分布。

### 8.6.1 先验的工程直觉

$p(z)$ 像一个动作风格池。

推理时，模型先从这个风格池里选一种模式，再根据当前条件生成动作。

但要注意：

```text
从 p(z) 采样不代表可以随便执行。
```

真实机器人中，采样出的动作还要经过碰撞检测、限位检查、轨迹平滑、安全规则和任务约束筛选。

---

## 8.7 条件生成策略：给定状态和 z 后生成动作

> **定义 8.3：隐变量条件策略**
>
> 隐变量条件策略用 $p_\theta(a\mid s,z)$ 或 $p_\theta(a\mid x,z)$ 表示：给定当前条件和隐变量后，生成动作的概率分布。

**公式 (8.6)：基于状态和隐变量的动作生成**

$$a\sim p_\theta(a\mid s,z)$$

更一般地，可以把输入写成条件 $x$：

**公式 (8.7)：基于条件和隐变量的动作生成**

$$a\sim p_\theta(a\mid x,z)$$

其中：

- $x$：策略输入条件，可以是状态、观测、历史、语言指令或多模态上下文；
- $z$：隐藏动作模式；
- $a$：生成的动作；
- $p_\theta(a\mid x,z)$：由参数 $\theta$ 控制的条件动作分布。

### 8.7.1 工程含义

同一个视觉观测 $x$ 下，不同 $z$ 可以生成不同动作：

- 一个 $z$ 生成左侧抓取动作；
- 一个 $z$ 生成右侧抓取动作；
- 一个 $z$ 生成保守接近动作；
- 一个 $z$ 生成快速接近动作。

这比直接用一个确定动作覆盖所有模式更自然。

---

## 8.8 边缘化：看不到 z 时如何得到动作分布？

真实数据中，我们通常看不到 $z$。如果不知道具体是哪种隐藏模式，就需要把所有可能的 $z$ 合起来。

> **定义 8.4：边缘化**
>
> 边缘化是指当某个隐变量不可观测时，对它的所有可能取值进行加权求和或积分，从而得到可观测变量的总体分布。

对于连续隐变量，动作分布可以写成：

**公式 (8.8)：边缘化隐变量得到动作分布**

$$p_\theta(a\mid x)=\int p_\theta(a\mid x,z)p(z)dz$$

这个公式读作：在条件 $x$ 下，动作 $a$ 的总概率等于在所有可能的隐变量 $z$ 上，把“给定 $z$ 时生成动作的概率”和“该 $z$ 出现的概率”加权积分起来。

其中：

- $p_\theta(a\mid x)$：不知道 $z$ 时的条件动作分布；
- $p_\theta(a\mid x,z)$：给定 $z$ 后的条件动作分布；
- $p(z)$：隐变量先验；
- $\int \cdot dz$：对所有可能的连续 $z$ 积分。

### 8.8.1 这不是把动作平均掉

很多人看到积分，会误以为这是把所有动作模式平均成一个动作。

不是。

它是在概率层面把多个模式组合成一个分布。只要模型表达能力足够，$p_\theta(a\mid x)$ 可以是多峰的，而不是一个均值点。

这正是隐变量策略相比确定性 MSE 的关键优势。

---

## 8.9 Posterior：如果看到动作，能反推出 z 吗？

> **定义 8.5：隐变量后验**
>
> 后验 $p(z\mid x,a)$ 表示在已经看到条件 $x$ 和动作 $a$ 后，隐变量 $z$ 可能是什么的概率分布。

如果看到某个专家动作，我们可以反过来问：

```text
这个动作可能来自哪种隐藏风格？
```

理论上可以写成：

**公式 (8.9)：隐变量后验**

$$p_\theta(z\mid x,a)=\frac{p_\theta(a\mid x,z)p(z)}{p_\theta(a\mid x)}$$

这个公式读作：在看到条件 $x$ 和动作 $a$ 后，某个隐变量 $z$ 的可能性，正比于该 $z$ 生成动作 $a$ 的概率乘以该 $z$ 本身的先验概率。

其中：

- $p_\theta(z\mid x,a)$：后验分布；
- $p_\theta(a\mid x,z)$：给定 $z$ 时生成动作的概率；
- $p(z)$：先验；
- $p_\theta(a\mid x)$：边缘动作概率。

### 8.9.1 为什么后验重要？

训练时我们看到专家动作 $a$，所以可以利用动作反推可能的 $z$。

推理时没有专家动作 $a$，所以不能继续用真实动作来推断 $z$。

这正是第9章 CVAE 的核心：

```text
训练时：encoder 可以看 x 和 a，推断 z；
推理时：没有 a，只能从 prior 或 conditional prior 采样 z。
```

---

## 8.10 每一步采样 z，还是整段轨迹保持 z？

隐变量策略有一个重要工程问题：$z$ 应该多久变化一次？

### 8.10.1 每一步重新采样

可以写成：

**公式 (8.10)：逐步采样隐变量**

$$z_t\sim p(z),\quad a_t\sim p_\theta(a_t\mid x_t,z_t)$$

这种方式灵活，但可能导致动作风格每一帧都变。

例如机械臂前一帧选择左抓风格，下一帧突然切到右抓风格，轨迹就可能抖动或自相矛盾。

### 8.10.2 一段轨迹共享 z

也可以让一整段轨迹共享同一个隐变量：

**公式 (8.11)：轨迹级隐变量**

$$z\sim p(z),\quad a_t\sim p_\theta(a_t\mid x_t,z),\quad t=0,1,\dots,T$$

这种方式更适合表达整体风格，比如：

- 这次抓取整体采用左侧接近；
- 这次泊车整体采用保守多次修正；
- 这次双臂操作整体采用左手先固定、右手后操作。

### 8.10.3 动作块共享 z

现代机器人策略中，也常让一个动作块共享 $z$：

**公式 (8.12)：动作块级隐变量**

$$z_t\sim p(z),\quad a_{t:t+H}\sim p_\theta(a_{t:t+H}\mid x_t,z_t)$$

这会在 ACT 等方法中再次出现。它的直觉是：不要每一步都重新纠结，而是一次生成一小段风格一致的动作。

---

## 8.11 隐变量策略的工程风险

### 8.11.1 z 不一定可解释

不要看到 latent 空间里有几团点，就立刻给每一团起名字叫“保守”“激进”“聪明”“优雅”。

$z$ 是模型为优化目标学出的结构，它不一定等同于人类理解的风格。

要判断 $z$ 是否有意义，需要做：

- 插值实验；
- 聚类可视化；
- 固定 $z$ 的 rollout；
- 改变 $z$ 后观察动作是否稳定变化；
- 检查闭环成功率和失败类型。

### 8.11.2 z 可能学到脏因素

如果数据里混有标定误差、控制延迟、失败轨迹、相机抖动、操作者手滑，模型可能把这些因素也编码进 $z$。

模型不会自动知道哪些差异叫“风格”，哪些差异叫“事故前兆”。

### 8.11.3 z 可能被模型忽略

如果 decoder 太强，或者 KL 约束太强，模型可能直接根据 $x$ 生成动作，而忽略 $z$。

表现为：

- 不同 $z$ 生成动作几乎一样；
- latent 插值没有明显变化；
- 多次采样动作缺乏多样性；
- 多模态数据仍然被平均。

这会在第9章 CVAE 的 posterior collapse 中继续讨论。

### 8.11.4 多样性不等于安全性

隐变量策略能生成多个候选动作，但并不保证每个候选都安全。

真实系统仍然需要：

- 碰撞检测；
- 关节限位；
- 速度和加速度限制；
- 轨迹平滑检查；
- 控制器可跟踪性检查；
- fallback 策略。

策略负责提供可能性，工程系统负责提供边界。

---

## 8.12 读完本章，你应该能判断什么？

读完本章后，你应该能形成以下判断：

1. **判断什么时候需要隐变量**：当动作差异背后存在未标注的风格、意图、阶段或专家习惯时，隐变量可能有价值。
2. **判断 z 和动作的区别**：$z$ 不是动作本身，而是影响动作生成的隐藏模式。
3. **判断 z 是否可解释**：latent 聚类不等于真实语义，需要通过干预实验和 rollout 验证。
4. **判断边缘化的含义**：积分不是把动作平均成一个点，而是把多个潜在模式组合成动作分布。
5. **判断训练和推理的差异**：训练时可以利用动作反推 $z$，推理时不能偷看专家动作。
6. **判断 z 的时间尺度**：逐步采样更灵活，轨迹级或动作块级 $z$ 更容易保持风格一致。
7. **判断隐变量策略的风险**：$z$ 可能被忽略、学到脏因素，或生成不安全候选。

---

## 8.13 本章小结

本章把第7章的概率策略进一步推进到隐变量策略。

核心要点如下：

1. 多模态动作往往来自隐藏风格、意图、任务阶段或专家习惯；
2. 隐变量 $z$ 是数据中没有直接标注、但影响动作生成的隐藏因素；
3. 先验 $p(z)$ 描述推理时可以从哪里采样隐藏模式；
4. 条件策略 $p_\theta(a\mid x,z)$ 表示给定条件和隐藏模式后生成动作；
5. 边缘化 $z$ 得到 $p_\theta(a\mid x)$，不是把动作平均掉，而是组合多个模式；
6. 后验 $p(z\mid x,a)$ 表示看到动作后反推隐藏模式；
7. $z$ 可以按步、按动作块或按整条轨迹采样；
8. 隐变量策略提供表达能力，但不自动保证解释性和安全性。

本章最重要的一句话是：

> 隐变量 $z$ 给多种合理动作提供了一个数学容器，但这个容器里装的是模型学到的隐藏结构，不一定天然等于人类理解的风格标签。

下一章会回答一个关键问题：训练数据里没有 $z$ 标签，模型到底怎么学会这个隐藏变量？这就是 CVAE 要解决的问题。

---

## 8.14 本章公式索引

### 公式 (8.1)：观测到的模仿学习样本

$$(s,a)$$

- **含义**：模仿学习数据中常见的状态—动作样本形式。
- **需要掌握到什么程度**：理解数据里通常只有状态和动作，没有隐藏风格标签。

### 公式 (8.2)：条件—动作样本

$$(x,a)$$

- **含义**：用 $x$ 统一表示状态、观测、历史、语言或多模态上下文。
- **需要掌握到什么程度**：理解现代机器人策略输入不一定只是状态 $s$。

### 公式 (8.3)：连续隐变量空间

$$z\in\mathbb{R}^d$$

- **含义**：隐变量可以是一个连续向量。
- **需要掌握到什么程度**：理解 latent 维度不一定有直接人类语义。

### 公式 (8.4)：从先验采样隐变量

$$z\sim p(z)$$

- **含义**：推理时从先验分布中采样隐藏模式。
- **需要掌握到什么程度**：理解推理时没有专家动作，通常只能从 prior 或 conditional prior 得到 $z$。

### 公式 (8.5)：标准高斯先验

$$p(z)=\mathcal{N}(0,I)$$

- **含义**：常见的连续 latent 先验。
- **需要掌握到什么程度**：知道这是 CVAE 中最常见的简单 prior。

### 公式 (8.6)：基于状态和隐变量的动作生成

$$a\sim p_\theta(a\mid s,z)$$

- **含义**：动作由状态和隐藏模式共同决定。
- **需要掌握到什么程度**：理解同一个状态下不同 $z$ 可以产生不同动作模式。

### 公式 (8.7)：基于条件和隐变量的动作生成

$$a\sim p_\theta(a\mid x,z)$$

- **含义**：更一般地，动作由上下文条件 $x$ 和隐变量 $z$ 共同生成。
- **需要掌握到什么程度**：为第9章 CVAE 的 decoder 做准备。

### 公式 (8.8)：边缘化隐变量得到动作分布

$$p_\theta(a\mid x)=\int p_\theta(a\mid x,z)p(z)dz$$

- **含义**：看不到 $z$ 时，把所有可能隐藏模式加权合成动作分布。
- **需要掌握到什么程度**：理解边缘化不是动作平均，而是模式组合。

### 公式 (8.9)：隐变量后验

$$p_\theta(z\mid x,a)=\frac{p_\theta(a\mid x,z)p(z)}{p_\theta(a\mid x)}$$

- **含义**：看到条件和动作后，反推隐藏模式的概率分布。
- **需要掌握到什么程度**：理解 CVAE encoder 为什么训练时可以看动作。

### 公式 (8.10)：逐步采样隐变量

$$z_t\sim p(z),\quad a_t\sim p_\theta(a_t\mid x_t,z_t)$$

- **含义**：每个时间步重新采样隐藏模式。
- **需要掌握到什么程度**：理解这种方式灵活但可能导致动作风格抖动。

### 公式 (8.11)：轨迹级隐变量

$$z\sim p(z),\quad a_t\sim p_\theta(a_t\mid x_t,z),\quad t=0,1,\dots,T$$

- **含义**：一整段轨迹共享同一个隐藏模式。
- **需要掌握到什么程度**：理解它适合表达长期一致的操作风格。

### 公式 (8.12)：动作块级隐变量

$$z_t\sim p(z),\quad a_{t:t+H}\sim p_\theta(a_{t:t+H}\mid x_t,z_t)$$

- **含义**：一个动作块共享一个隐变量。
- **需要掌握到什么程度**：为 ACT 的 action chunk latent 做准备。

---

## 8.15 本章定义索引

| 编号 | 概念 | 一句话含义 |
|---|---|---|
| 定义 8.1 | 隐变量 | 数据中未直接观测但影响动作生成的隐藏因素 |
| 定义 8.2 | 隐变量先验 | 推理前隐变量可能取值的分布 |
| 定义 8.3 | 隐变量条件策略 | 给定条件和隐变量后生成动作的策略分布 |
| 定义 8.4 | 边缘化 | 对不可观测隐变量求和或积分以得到总体分布 |
| 定义 8.5 | 隐变量后验 | 看到条件和动作后反推隐变量的分布 |

---

## 8.16 建议阅读的附录条目

1. **附录 B：概率论最小生存包**
   - 阅读目的：理解先验、后验、条件概率和边缘化。
   - 对应本章：帮助理解 $p(z)$、$p(z\mid x,a)$ 和 $p_\theta(a\mid x)$。

2. **附录 C：最大似然、负对数似然、交叉熵与 KL 散度**
   - 阅读目的：为第9章 CVAE 的 likelihood、ELBO 和 KL 项做准备。
   - 对应本章：帮助理解为什么隐变量模型最终仍然要通过概率目标训练。

3. **附录 G：生成模型基础**
   - 阅读目的：理解 latent variable model 的基本思想。
   - 对应本章：帮助把隐变量策略放进生成模型框架中。

---

## 8.17 思考题

1. 在一个抓取任务中，哪些动作差异可能来自真实风格，哪些可能只是数据噪声或标定误差？
2. 如果 $z$ 被解释成“左抓/右抓”，你会设计什么实验验证这个解释是否成立？
3. 为什么边缘化 $z$ 不等于简单平均动作？请用左抓/右抓例子解释。
4. 逐步采样 $z_t$ 和轨迹级共享 $z$，分别适合什么任务？
5. 如果不同 $z$ 生成的动作几乎一样，你会怀疑模型出了什么问题？
6. 如果 latent 聚类结果很好看，但闭环成功率没有提升，这说明什么？
7. 为什么第9章需要 CVAE？训练数据里没有 $z$ 标签时，模型该如何学习隐变量？

---

## 8.18 本章配图清单

本章复用并保留 1 张核心概念图：

1. **图8-1 隐变量像动作风格旋钮**：解释同一状态下不同 $z$ 可以对应不同动作风格。

如果后续补图，建议增加：

2. **图8-2 边缘化 z 得到多峰动作分布**：解释积分不是动作平均，而是组合多个模式；
3. **图8-3 逐步 latent、动作块 latent、轨迹级 latent 对比图**：解释不同时间尺度的隐变量。

---

## 8.19 推荐阅读与深入材料

### 8.19.1 Kingma and Welling, Auto-Encoding Variational Bayes

- **类型**：经典论文。
- **阅读目的**：理解 VAE 中隐变量、先验、后验和重参数化的来源。
- **重点看什么**：latent variable model、encoder、decoder、ELBO。
- **对应本章**：为第9章 CVAE 做理论准备。

### 8.19.2 Sohn, Lee, and Yan, Learning Structured Output Representation using Deep Conditional Generative Models

- **类型**：CVAE 经典论文。
- **阅读目的**：理解条件生成模型如何使用隐变量表达多种输出。
- **重点看什么**：conditional latent variable、CVAE objective。
- **对应本章**：直接对应 $p_\theta(a\mid x,z)$ 和 $q_\phi(z\mid x,a)$。

### 8.19.3 ACT: Learning Fine-Grained Bimanual Manipulation with Low-Cost Hardware

- **类型**：机器人模仿学习论文。
- **阅读目的**：观察隐变量如何用于动作块和双臂操作。
- **重点看什么**：CVAE、action chunk、latent variable 的使用方式。
- **对应本章**：说明隐变量不仅可以表示单步动作风格，也可以表示一段动作序列的模式。

### 8.19.4 Diffusion Policy 相关论文

- **类型**：现代生成式策略论文。
- **阅读目的**：理解更强的生成模型如何表达复杂动作分布。
- **重点看什么**：action distribution、multimodality、trajectory generation。
- **对应本章**：帮助读者看到隐变量策略与后续生成式策略之间的连续关系。
