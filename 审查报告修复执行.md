# 审查报告修复执行记录

> 本文件记录本轮按照审查报告进行的修复执行状态。由于当前 GitHub 写入接口只支持整文件替换，不支持局部 patch，为避免误伤大章节正文，本轮先提交可执行、幂等的自动修复脚本：`scripts/apply_review_report_fixes.py`。

## 一、本轮已提交

- 新增脚本：`scripts/apply_review_report_fixes.py`
- 提交用途：自动执行审查报告中的机械修复、补齐体验项、生成一致性扫描结果。

## 二、脚本覆盖范围

### A. 立即执行机械修复

1. ch2：移出 blockquote 内公式块。
   - 处理方式：只移除 `$$...$$` 公式块内部行首的 `> `，命题说明文字不动。
   - 目的：满足公式不放在 blockquote 内的格式规范。

2. ch16：范数写法统一为 `\lVert...\rVert`。
   - 处理方式：将 `\left\|` 替换为 `\lVert`，将 `\right\|` 替换为 `\rVert`。
   - 目的：统一范数格式，避免渲染器对竖线宏解析不一致。

### B. 补齐体验项

3. ch11：补思考题 + 本章配图清单。
   - 插入位置：原“建议阅读的附录条目”之前。
   - 后续章节编号自动顺延。

4. ch12：补思考题 + 本章配图清单。
   - 插入位置：原“建议阅读的附录条目”之前。
   - 后续章节编号自动顺延。

### C. 一致性扫描

5. 扫描仍使用 Markdown 表格的公式索引。
6. 扫描 ch22 / ch23 / 图22 / 图23 等旧引用。
7. 生成图片命名映射表 `图片命名映射表.md`。
8. 为最后批量统一图片引用提供依据。

## 三、运行方式

在仓库根目录执行：

```bash
python scripts/apply_review_report_fixes.py --scan
```

只扫描，不写入源文件。

```bash
python scripts/apply_review_report_fixes.py --write
```

执行修复，并生成：

- `审查报告修复执行.md`
- `图片命名映射表.md`

## 四、为什么采用脚本而不是直接整文件替换

ch2 和 ch16 都是数百行章节文件。当前可用 GitHub 写入接口是整文件替换模式，不支持局部 patch。直接由对话中重组整章内容，存在误删、漏行、破坏原章节结构的风险。

因此本轮采用更稳妥方式：

```text
机械修复规则脚本化
→ 本地或 Codex 执行脚本
→ 由脚本只改命中的局部文本
→ 自动生成扫描报告和图片映射表
```

## 五、下一步

1. 运行 `python scripts/apply_review_report_fixes.py --write`。
2. 检查 git diff，重点看 ch2、ch16、ch11、ch12。
3. 打开生成的 `图片命名映射表.md`，逐条确认是否需要重命名图片。
4. 对 ch22/ch23 旧引用扫描结果逐条判断：正书固定为六篇二十一章时，应改为 ch20/ch21 或移入存档说明。
