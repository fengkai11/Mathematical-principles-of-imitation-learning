# 图片命名映射表

> 本表用于最后统一图片命名和引用。完整映射可通过 `python scripts/apply_review_report_fixes.py --write` 自动生成。

## 一、命名规则

正书图片统一建议采用：

```text
图X-Y_简短中文说明.svg
图X-Y_简短中文说明.png
```

其中：

- `X` 为章节号；
- `Y` 为本章内图片序号；
- 中文说明应短，不写长句；
- 优先使用与正文图注一致的语义；
- 不再保留重排前的旧章节号，例如 `图22-*`、`图23-*`。

## 二、自动生成字段

脚本会扫描章节中的图片引用，并生成以下字段：

| 引用文件 | 行号 | 图注 alt | 当前图片文件 | 文件是否存在 | 建议文件名 | 状态 |
|---|---:|---|---|---|---|---|
| 待脚本生成 | - | - | - | - | - | - |

## 三、统一原则

1. 先确认图片内容确实对应图注，再改文件名和引用。
2. 如果一个图片文件被多个章节复用，优先保留语义最准确的文件名，不盲目按引用处章节号重命名。
3. 如果图注是“图20-1”，但文件名仍是“图22-1”，优先判断是否为章节重排遗留。
4. 图片文件名统一后，要同步修改章节 Markdown 中的 `../images/...` 引用。
5. 最后统一检查：图号、图注、文件名、SUMMARY 章节号是否一致。

## 四、执行命令

```bash
python scripts/apply_review_report_fixes.py --write
```

运行后，本文件会被脚本替换为完整映射表。
