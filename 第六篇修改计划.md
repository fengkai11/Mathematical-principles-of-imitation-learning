# 第六篇修改计划：从动作模仿到世界预测

> 本计划用于指导《模仿学习的数学原理》第六篇的后续改造。
>
> 新定位：第六篇不是“额外讲认知架构”，也不把“快慢系统”作为主线，而是说明：模仿学习不能只学“专家做了什么动作”，还要学习“动作会让世界如何变化”，并用这种预测能力辅助计划、验证、安全判断和失败恢复。

---

## 0. 核心定位

第六篇主线收敛为：

```text
从“模仿动作”
到“预测动作后果”
再到“用预测辅助策略执行”
```

对应数学主线为：

```text
πθ(a_t | o_t)
→ πθ(a_t | o_{≤t}, q)
→ p(h_{t+1} | h_t, a_t)
→ ĥ_{t+1}=F_θ(h_t,a_t)
→ 用世界预测辅助计划、验证、安全判断和失败恢复
```

第六篇要回答的核心问题是：

```text
模仿学习不能只学“专家做了什么动作”，
还要理解“这个动作会让世界变成什么”。
```

快慢系统只作为补充内容：

```text
当世界预测和任务规划无法满足低层控制频率时，
工程上通常需要时间尺度分工、异步缓存和 fallback。
```

因此，快慢系统不再作为第六篇主标题，也不作为主线关键词。

---

## 1. 第六篇标题建议

当前标题：

```text
第六篇：世界模型、快慢系统与认知架构
```

建议改为：

```text
第六篇：从动作模仿到世界预测
```

备选标题：

```text
第六篇：世界模型与动作后果预测
```

或者：

```text
第六篇：动作后果预测与世界模型
```

最推荐第一种，因为它和前文“模仿动作”的主线衔接最自然。

---

## 2. 第六篇总主线

第六篇不再强调“认知架构”，而是围绕动作后果预测展开。

主线关系：

```text
前五篇：如何从专家数据中学习动作策略
第六篇：为什么动作策略还不够，必须学习动作后果
第七篇：如何用偏好、接管和后训练继续改进策略系统
```

第六篇要避免写成：

```text
世界模型综述
VLA / VLM 前沿介绍
机器人认知架构畅想
Kahneman 快慢系统类比
```

而要写成：

```text
动作模仿的不足
→ 世界预测的必要性
→ 世界预测的数学对象
→ 预测误差与控制效果的关系
→ 世界预测如何服务计划、验证、安全判断和失败恢复
```

---

## 3. 修改后的第六篇结构

建议调整为：

```text
第六篇：从动作模仿到世界预测

第21章：从动作模仿到世界预测：机器人不能只会背动作表
第22章：World Models、World Action Models 与 JEPA：机器人要学会预测动作后果
第23章：世界预测如何进入机器人策略系统：计划、验证与安全边界
```

三章关系：

```text
第21章：为什么需要世界预测
第22章：世界预测的数学对象和训练目标
第23章：世界预测如何服务策略执行
```

也就是：

```text
只学动作不够
→ 必须预测动作后果
→ 用预测辅助策略系统
```

---

## 4. 第六篇导读修改计划

### 4.1 开篇重新定位

导读开头建议改为：

```text
前五篇主要回答：如何从专家数据中学习动作策略。

但是，真实机器人系统只会模仿动作还不够。它还需要回答三个更难的问题：

第一，如果我这样做，世界会发生什么？
第二，哪些动作后果是安全、可恢复、可继续执行的？
第三，预测到的后果如何帮助策略执行、计划验证和失败恢复？

因此，第六篇不是离开模仿学习主线，而是把模仿学习从动作预测扩展到动作后果预测。
```

### 4.2 明确本篇数学对象

导读中要明确本篇引入的数学对象：

```text
1. 动作策略：
   πθ(a_t | o_{≤t}, q)

2. 未来状态预测：
   p(h_{t+1} | h_t, a_t)

3. 确定性 latent dynamics：
   ĥ_{t+1}=F_θ(h_t,a_t)

4. 世界预测辅助策略执行：
   候选动作评估、短期 rollout、风险预测、失败恢复
```

### 4.3 明确三章分工

导读中加入：

```text
第21章说明为什么“只学动作”不够，引出动作后果预测问题。
第22章正式定义 World Model、World Action Model 和 JEPA 式表示预测，建立世界预测的数学核心。
第23章讨论世界预测如何进入机器人策略系统，用于计划、验证、安全判断和失败恢复。
```

---

## 5. 第21章修改计划

### 5.1 当前定位

第21章是第六篇的问题引出章。

它不应该承担太多 World Model 技术细节，也不应该写成前沿综述。它只需要证明一件事：

```text
只学习动作策略 πθ(a | o) 还不够。
```

### 5.2 建议标题

当前标题可以保留：

```text
第21章：从动作模仿到世界理解：机器人不能只会背动作表
```

也可以微调为：

```text
第21章：从动作模仿到世界预测：机器人不能只会背动作表
```

建议使用“世界预测”，少用“世界理解”。“世界理解”偏泛，“世界预测”更贴近数学对象。

### 5.3 章节主线

第21章内部主线收敛为：

```text
动作策略只能回答“现在该怎么做”
但不能显式回答“这样做会发生什么”
所以需要引入动作后果预测
```

### 5.4 开头压缩

不要大篇幅讲“机器人要理解世界”。应该直接从前文公式进入：

```text
前面我们学习了各种策略形式：

πθ(a_t | o_t)
πθ(A_t | o_{t-L:t})
πθ(a_t | o_{≤t}, q)

这些公式都在回答“当前应该输出什么动作”。

但它们没有显式回答：

如果执行动作 a_t，下一步世界会变成什么？
```

### 5.5 核心公式过渡

建议新增或强化：

```markdown
**公式 (21.1)：动作策略**

$$a_t \sim \pi_\theta(a_t \mid o_{\le t}, q)$$

**公式 (21.2)：动作后果预测**

$$\hat h_{t+1}=F_\phi(h_t,a_t)$$
```

解释：

```text
公式 (21.1) 学的是“怎么做”。
公式 (21.2) 学的是“这样做之后会怎样”。
```

### 5.6 强化命题 21.1

第21章最重要命题应围绕：

```text
只从专家动作数据学习 π(a|o)，无法唯一识别动作后果模型。
```

建议命题写成：

```text
命题 21.1：相同专家动作分布不唯一决定世界动力学

存在两个不同环境动力学 P_1 和 P_2，使得专家在观测分布上的动作表现相同，但同一个动作在两个世界中的后果不同。
因此，仅靠动作模仿目标无法唯一识别动作后果。
```

这就是“不可识别性 / 多世界反例”，是第21章最重要的数学桥梁。

### 5.7 第21章结尾

结尾必须引出第22章：

```text
第21章说明了：动作模仿不能自动给出动作后果预测。
第22章将正式定义 World Model、World Action Model 和 JEPA 式表示预测，回答“预测什么、条件是什么、怎么训练”。
```

---

## 6. 第22章修改计划

### 6.1 当前定位

第22章是第六篇的数学核心章。

它必须承担：

```text
把“世界预测”从直觉变成数学对象。
```

### 6.2 标题建议

当前标题可以保留，但建议略微聚焦：

```text
第22章：World Models、World Action Models 与 JEPA：机器人要学会预测动作后果
```

“预测动作后果”比“预测世界”更贴近模仿学习主线。

### 6.3 必须回答的四个问题

第22章必须回答：

```text
1. 预测什么？
2. 条件是什么？
3. 训练目标是什么？
4. 预测误差和控制效果是什么关系？
```

### 6.4 核心公式主线

建议收敛为：

```text
状态转移：
p(s_{t+1} | s_t, a_t)

latent dynamics：
p(h_{t+1} | h_t, a_t)

确定性表示预测：
ĥ_{t+1}=F_θ(h_t,a_t)

多步预测：
ĥ_{t+H}=F_θ^{(H)}(h_t,a_{t:t+H-1})

预测损失：
L_pred = ||ĥ_{t+1}-h_{t+1}||²

动作条件世界预测：
ĥ_{t+1}^{(a)} = F_θ(h_t,a_t)

JEPA 式表示预测：
ŷ = f_θ(x, a)
```

### 6.5 最重要命题

保留并强化：

```text
命题 22.1：预测误差小不必然推出控制效果好
```

证明方向：

```text
预测误差是表示空间或状态空间中的平均误差；
控制效果取决于任务相关变量、安全边界、接触阈值和长期误差累积；
因此，小的平均预测误差不必然保证策略执行成功。
```

这个命题非常关键，因为它防止第六篇变成：

```text
有世界模型就解决一切。
```

### 6.6 必须补工程边界

第22章要明确：

```text
1. 短期预测可用，不代表长期 rollout 可用；
2. 表示预测准确，不代表接触状态预测准确；
3. 视觉预测好，不代表控制安全；
4. 世界模型可以辅助计划，但不能替代实机验证；
5. 预测误差必须和任务风险、失败阈值、fallback 绑定。
```

### 6.7 第22章结尾

结尾要引出第23章：

```text
第22章解决了“如何预测动作后果”。

但它还没有解决：

世界预测学到之后，真实机器人系统应该如何使用它？

这正是第23章要讨论的问题。
```

---

## 7. 第23章修改计划

### 7.1 新定位

第23章不再作为“快慢模型章”。

新的定位是：

```text
世界预测如何进入机器人策略系统？
```

它的职责是：

```text
世界模型学到了动作后果预测之后，真实机器人系统怎么使用它？
```

### 7.2 标题建议

当前标题：

```text
第23章：快慢模型：通用机器人为什么需要双系统架构
```

建议改为：

```text
第23章：世界预测如何进入机器人策略系统：计划、验证与安全边界
```

备选：

```text
第23章：从世界预测到策略执行：计划、验证与安全边界
```

### 7.3 第23章主线

第23章主线应该是：

```text
世界模型如何被用起来？
```

而不是：

```text
为什么需要快慢系统？
```

建议结构：

```text
0. 本章要解决的问题：世界预测学到之后，怎么服务策略执行？
1. 与第22章和第24章的衔接
2. 本章核心定义
   - 世界预测模块
   - 候选动作评估
   - 短期 rollout
   - 风险预测
   - 失败恢复
3. 本章公式主线
4. 用世界模型评估候选动作
5. 用世界模型做短期 rollout
6. 用世界模型预测风险和失败边界
7. 用世界模型辅助失败恢复
8. 世界预测与策略执行的接口
9. 补充：时间尺度、延迟约束与异步缓存
10. 与 Preference Alignment / DPO 的关系
11. 统一例子：机械臂抓取与放置
12. 常见误解与适用边界
13. 读完本章，你应该能判断什么
14. 本章公式索引
15. 本章定义索引
16. 思考题
17. 本章配图清单
18. 建议阅读的附录条目
19. 推荐阅读与深入材料
20. 本章小结
```

### 7.4 保留但降权的内容

之前第23章中已经写好的内容不是没用，但要降权。

保留：

```text
延迟约束
异步缓存
fallback
慢预测 / 快执行
```

但放入：

```text
补充：时间尺度、延迟约束与异步缓存
```

不要再作为全章主标题和核心叙事。

### 7.5 新增核心内容

第23章要围绕“世界预测如何被使用”新增内容。

#### 7.5.1 候选动作评估

```text
给定候选动作 a_t^{(1)}, a_t^{(2)}, ...，世界模型预测它们的后果，再用任务评分或风险评分选择。
```

示例公式：

```text
ĥ_{t+1}^{(j)} = F_θ(h_t,a_t^{(j)})

a_t^* = arg max_{a_t^{(j)}} J(ĥ_{t+1}^{(j)}, q)
```

#### 7.5.2 短期 rollout

```text
世界模型不只预测一步，也可以预测短 horizon 后果，用于判断某段动作是否可行。
```

示例公式：

```text
ĥ_{t+\ell+1}=F_θ(ĥ_{t+\ell},a_{t+\ell}), \quad \ell=0,\dots,H-1
```

#### 7.5.3 风险预测

```text
预测结果可以进入风险函数，例如距离障碍物、接触状态、任务失败概率等。
```

示例公式：

```text
R_t = r_ψ(ĥ_{t+1}, q)
```

#### 7.5.4 失败恢复

```text
当预测结果显示当前动作可能失败，系统可以选择重规划、切换策略、触发 fallback 或请求人工接管。
```

示例公式：

```text
b_t = 1\{R_t > R_{max}\}
```

#### 7.5.5 时间尺度补充

放在较后位置：

```text
如果世界预测模型推理较慢，而低层控制频率较高，工程上通常需要异步缓存、时间尺度分工和 fallback。
```

---

## 8. 三章之间的接力关系

修改后，第六篇三章应该形成这条链：

```text
第21章：
只学动作 πθ(a | o) 不够，因为它没有显式学习动作后果。

第22章：
引入世界预测 p(h_{t+1} | h_t, a_t)，让机器人学习“这样做会发生什么”。

第23章：
世界预测学到之后，可以用于候选动作评估、短期 rollout、风险预测、失败恢复和策略执行接口。
```

也就是：

```text
动作模仿的不足
→ 世界预测的必要性
→ 世界预测的使用方式
```

---

## 9. 需要删除或弱化的内容

### 9.1 弱化“认知架构”表述

“认知架构”可以保留在推荐阅读或小结里，但不要作为主线标题和章节主语频繁出现。

推荐替换：

```text
认知架构
```

改为：

```text
世界预测
动作后果预测
机器人策略系统
计划、验证与安全边界
```

### 9.2 弱化大模型前沿罗列

不要让第六篇变成：

```text
RT-2 / OpenVLA / π0 / Gemini Robotics / Figure / Physical Intelligence 综述
```

这些可以作为例子，但不能主导章节。

### 9.3 弱化心理学快慢系统类比

Kahneman 可以放推荐阅读，但正文不要用它当理论基础。

正文理论基础应该是：

```text
动作后果预测
状态转移
latent dynamics
候选动作评估
短期 rollout
风险预测
失败恢复
```

### 9.4 降权快慢系统

快慢系统保留为第23章补充内容，不再作为第六篇主线。

正文可以这样表述：

```text
当世界预测和任务规划无法满足低层控制频率时，工程上通常需要时间尺度分工、异步缓存和 fallback。
```

不要再把第六篇写成：

```text
世界模型、快慢系统与认知架构
```

---

## 10. P0 / P1 / P2 执行清单

### P0：主线收敛

```text
1. 第六篇标题改为“从动作模仿到世界预测”。
2. 第六篇导读删除“快慢系统与认知架构”作为主线表述。
3. 第21章聚焦“只学动作不够”。
4. 第22章强化为“世界预测数学核心章”。
5. 第23章从“快慢模型章”改为“世界预测如何进入策略系统”。
```

### P1：内容重构

```text
1. 第21章补不可识别性 / 多世界反例。
2. 第22章补预测误差小不等于控制效果好。
3. 第23章重写为：
   - 候选动作评估；
   - 短期 rollout；
   - 风险预测；
   - 失败恢复；
   - 预测模块与控制模块的接口；
   - 补充：时间尺度和延迟约束。
4. 三章结尾都明确写“本章如何接下一章”。
5. 全篇统一符号：
   - h_t：latent state；
   - a_t：低层动作；
   - u_k：高层动作或子目标，若第23章保留时间尺度补充时使用；
   - z_k：快模型条件，若第23章保留时间尺度补充时使用；
   - \bar z_t：异步缓存条件，若第23章保留时间尺度补充时使用。
```

### P2：体验优化

```text
1. 第六篇导读补数学主线图。
2. 第21章图：动作模仿 vs 动作后果预测。
3. 第22章图：状态预测 / latent 预测 / JEPA 表示预测。
4. 第23章图：世界模型如何辅助策略执行。
5. 第六篇篇末补“读完第六篇，你应该能判断什么”。
```

---

## 11. 最终改造验收标准

第六篇改完后，读者应该能清楚回答：

```text
1. 为什么 πθ(a | o) 只回答“怎么做”，没有回答“这样做会怎样”？
2. 为什么需要 p(h_{t+1} | h_t, a_t) 这样的世界预测对象？
3. World Model、World Action Model 和 JEPA 分别在预测什么？
4. 为什么世界模型预测误差小，不等于机器人控制一定成功？
5. 世界预测如何用于候选动作评估、短期 rollout、风险预测和失败恢复？
6. 为什么世界预测不能替代真实控制器、安全边界和实机验证？
7. 快慢系统为什么只是工程补充，而不是第六篇数学主线？
8. 为什么 Preference Alignment / DPO 可以进一步作用在计划选择和动作轨迹选择上？
```

如果这些问题都能回答，第六篇就在“模仿学习数学原理”的主线上。

---

## 12. 一句话原则

第六篇最终要达到：

```text
模仿学习不能只学“专家做了什么动作”，
还要学习“动作会让世界如何变化”，
并用这种预测能力辅助计划、验证、安全判断和失败恢复。
```
